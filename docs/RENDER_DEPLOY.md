# 🚀 Деплой FlowBox на Render.com с админ-панелью и БД

## 📋 Подготовка

### 1. Создание базы данных на Render

1. Зайдите на https://render.com
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Настройки:
   - **Name:** `flowbox-db`
   - **Database:** `flowbox`
   - **User:** (автоматически)
   - **Region:** Выберите ближайший (например, Frankfurt)
   - **Plan:** Free (или Starter для production)
4. Нажмите **"Create Database"**
5. Дождитесь создания БД (1-2 минуты)
6. Скопируйте **Internal Database URL** или **External Database URL**

### 2. Подготовка схемы базы данных

Выполните SQL скрипты из `database/schema.sql` в вашей БД:
- Откройте **"Connect"** в настройках БД
- Используйте **psql** или **pgAdmin** для подключения
- Выполните все команды из `database/schema.sql`

Или используйте Render Shell:
```bash
# В Render Dashboard → Database → Connect → Shell
psql $DATABASE_URL
# Затем выполните SQL команды из schema.sql
```

## 🔧 Настройка деплоя

### Вариант 1: Через render.yaml (Рекомендуется)

1. Убедитесь, что файл `render.yaml` в корне проекта
2. В Render Dashboard:
   - **"New +"** → **"Blueprint"**
   - Подключите GitHub репозиторий
   - Render автоматически обнаружит `render.yaml`

### Вариант 2: Через Web Service

1. **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий
3. Настройки:
   - **Name:** `flowbox-app`
   - **Environment:** `Node`
   - **Build Command:** `npm install && npm run build:admin`
   - **Start Command:** `npm start`
   - **Plan:** Free (или Starter для production)

## 🔐 Переменные окружения

В настройках Web Service добавьте:

### Обязательные:
- `BOT_TOKEN` - токен Telegram бота (из @BotFather)
- `DATABASE_URL` - URL базы данных (из настроек PostgreSQL)
  - Формат: `postgresql://user:password@host:port/database`
  - Можно взять из Render Dashboard → Database → Connect

### Автоматические (из render.yaml):
- `WEBAPP_URL` - автоматически устанавливается Render
- `PORT` - автоматически устанавливается Render (3000)

## 📦 Процесс сборки

Render выполнит следующие шаги:

1. **Установка зависимостей сервера:** `npm install`
2. **Установка зависимостей админ-панели:** `cd admin && npm install`
3. **Сборка React админ-панели:** `cd admin && npm run build`
4. **Запуск сервера:** `npm start`

Результат:
- Собранная админ-панель в папке `admin-build/`
- Express сервер раздает статические файлы
- Подключение к PostgreSQL базе данных

## ✅ Проверка деплоя

После успешного деплоя:

1. **Проверьте логи** в Render Dashboard:
   - Должно быть: `✅ Подключение к базе данных установлено`
   - Должно быть: `🚀 Сервер запущен на http://localhost:3000`

2. **Откройте админ-панель:**
   - URL: `https://your-app.onrender.com/admin`
   - Пароль: `admin123`

3. **Проверьте работу БД:**
   - В админ-панели должны отображаться товары из БД
   - Заказы должны сохраняться в БД

## 🐛 Решение проблем

### Админ-панель не собирается

**Проблема:** Ошибка при сборке React приложения

**Решение:**
1. Проверьте логи сборки в Render
2. Убедитесь, что в `admin/package.json` все зависимости указаны
3. Проверьте, что `admin/vite.config.js` настроен правильно

### База данных не подключается

**Проблема:** `DATABASE_URL не установлен`

**Решение:**
1. Проверьте, что переменная `DATABASE_URL` добавлена в Environment
2. Убедитесь, что используете правильный URL (Internal или External)
3. Для Render лучше использовать **Internal Database URL**

### Статические файлы не загружаются

**Проблема:** Админ-панель показывает пустую страницу

**Решение:**
1. Проверьте, что папка `admin-build/` создана после сборки
2. Убедитесь, что в `vite.config.js` указан `base: '/admin/'`
3. Проверьте логи Express на ошибки раздачи статики

### Данные не отображаются

**Проблема:** Админ-панель пустая, хотя данные есть в БД

**Решение:**
1. Проверьте подключение к БД в логах
2. Убедитесь, что таблицы созданы (`products`, `orders`, `users`)
3. Проверьте API endpoints в Network tab браузера
4. Убедитесь, что `authToken` передается в запросах

## 📊 Структура базы данных

Убедитесь, что созданы следующие таблицы:
- `products` - товары
- `orders` - заказы
- `order_items` - позиции заказов
- `users` - пользователи
- `addresses` - адреса доставки
- `couriers` - курьеры
- `delivery_zones` - зоны доставки
- `delivery_slots` - временные слоты

Все таблицы должны быть созданы через `database/schema.sql` и `database/schema-admin-expansion.sql`

## 🔄 Обновление после изменений

После изменений в коде:

1. **Закоммитьте изменения:**
```bash
git add .
git commit -m "Update admin panel"
git push
```

2. **Render автоматически пересоберет проект**

3. **Проверьте логи** на ошибки

## 💡 Полезные команды

### Локальная проверка перед деплоем:

```bash
# 1. Соберите админ-панель
cd admin && npm run build && cd ..

# 2. Проверьте, что admin-build создан
ls -la admin-build/

# 3. Запустите сервер локально
npm start

# 4. Проверьте админ-панель
open http://localhost:3000/admin
```

### Проверка подключения к БД:

```bash
# В Render Dashboard → Database → Connect → Shell
psql $DATABASE_URL

# Проверьте таблицы
\dt

# Проверьте данные
SELECT * FROM products LIMIT 5;
SELECT * FROM orders LIMIT 5;
```

## 🎯 Итоговый чеклист

- [ ] PostgreSQL база данных создана на Render
- [ ] Схема БД применена (выполнены SQL скрипты)
- [ ] Web Service создан и подключен к GitHub
- [ ] Переменные окружения настроены:
  - [ ] `BOT_TOKEN`
  - [ ] `DATABASE_URL`
- [ ] Build Command: `npm install && npm run build:admin`
- [ ] Start Command: `npm start`
- [ ] Деплой успешно завершен
- [ ] Админ-панель доступна по `/admin`
- [ ] Данные из БД отображаются в админ-панели

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Render Dashboard
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab для API запросов
4. Убедитесь, что все переменные окружения установлены

