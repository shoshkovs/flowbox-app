# 🐛 Диагностика белого экрана в админ-панели

## Возможные причины:

### 1. Админ-панель не собралась на Render

**Проверка:**
- Откройте Render Dashboard → Logs
- Найдите строку: `✓ built in ...` (должна быть после `npm run build:admin`)
- Если её нет - сборка не прошла

**Решение:**
- Проверьте Build Command: `npm install && npm run build:admin`
- Убедитесь, что в `admin/package.json` все зависимости указаны
- Проверьте логи на ошибки сборки

### 2. Папка admin-build не создалась

**Проверка в логах:**
Должна быть строка:
```
🔍 Проверка админ-панели:
  admin-build существует: true
```

Если `false` - папка не создалась.

**Решение:**
- Проверьте, что Build Command выполняется полностью
- Убедитесь, что команда `cd admin && npm run build` выполняется успешно
- Проверьте права доступа к файловой системе

### 3. Ошибки в React коде

**Проверка:**
- Откройте консоль браузера (F12)
- Проверьте вкладку Console на ошибки
- Проверьте вкладку Network - загружаются ли файлы `.js` и `.css`

**Решение:**
- Если есть ошибки в консоли - исправьте их
- Если файлы не загружаются - проверьте пути в `vite.config.js` (должен быть `base: '/admin/'`)

### 4. Неправильные пути к assets

**Проверка:**
- Откройте Network tab в браузере
- Проверьте запросы к `/admin/assets/...`
- Если 404 - проблема с путями

**Решение:**
- Убедитесь, что в `vite.config.js` указан `base: '/admin/'`
- Пересоберите: `cd admin && npm run build`

### 5. Проблемы с порядком маршрутов

**Проверка в логах:**
Должна быть строка:
```
📄 Запрос к /admin - отдаем index.html
```

Если её нет - маршрут не обрабатывается.

**Решение:**
- Убедитесь, что маршруты админки идут ДО статических файлов MiniApp
- Проверьте порядок в `bot.js`

## Быстрая диагностика:

1. **Откройте консоль браузера (F12)**
2. **Проверьте ошибки в Console**
3. **Проверьте Network tab:**
   - Загружается ли `index.html`?
   - Загружаются ли файлы из `/admin/assets/`?
   - Какие статусы у запросов?

4. **Проверьте логи Render:**
   - Есть ли `✓ built in ...`?
   - Есть ли `admin-build существует: true`?
   - Есть ли ошибки сборки?

## Что проверить в Render Dashboard:

1. **Build Command:** `npm install && npm run build:admin`
2. **Start Command:** `npm start`
3. **Environment Variables:**
   - `DATABASE_URL` установлен
   - `BOT_TOKEN` установлен

4. **Logs:**
   - Должна быть строка о сборке админ-панели
   - Должна быть строка о проверке admin-build
   - Не должно быть ошибок подключения к БД

## Если ничего не помогает:

1. **Проверьте локально:**
```bash
cd admin
npm run build
cd ..
npm start
# Откройте http://localhost:3000/admin
```

2. **Если локально работает, но на Render нет:**
   - Проверьте, что все файлы закоммичены и запушены
   - Убедитесь, что Build Command правильный
   - Проверьте версии Node.js на Render

3. **Временное решение:**
   - Можно временно использовать старую HTML версию
   - Удалите папку `admin-build` на Render (если есть)
   - Сервер автоматически переключится на старую версию

