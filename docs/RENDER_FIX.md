# 🔧 Исправление ошибки "Cannot find module 'pg'" в Render.com

## Проблема
Render.com не устанавливает библиотеку `pg` перед запуском приложения.

## Решение

### Вариант 1: Проверь Build Command в Render.com (РЕКОМЕНДУЕТСЯ)

1. Зайди в Render.com → твой веб-сервис `flowbox-app`
2. Перейди в раздел **Settings**
3. Найди раздел **Build & Deploy**
4. Проверь **Build Command** - должно быть:
   ```
   npm install
   ```
   или
   ```
   npm ci
   ```
5. Если команда пустая или неправильная - измени на `npm install`
6. Сохрани изменения
7. Render автоматически перезапустит деплой

### Вариант 2: Ручной перезапуск деплоя

1. Зайди в Render.com → твой веб-сервис `flowbox-app`
2. Нажми кнопку **"Manual Deploy"** → **"Deploy latest commit"**
3. Подожди завершения деплоя

### Вариант 3: Проверь, что package.json в корне проекта

Убедись, что файл `package.json` находится в корне репозитория (не в подпапке).

---

## Проверка после исправления

После перезапуска деплоя в логах должно появиться:

```
✅ Подключение к базе данных установлено и протестировано
```

Если все еще видишь ошибку `Cannot find module 'pg'`:
1. Проверь, что `package.json` содержит `"pg": "^8.11.3"` в dependencies
2. Проверь, что Build Command = `npm install`
3. Проверь логи деплоя - должна быть строка `npm install` с установкой пакетов

