# 🔧 Исправление ошибки "column updated_at does not exist"

## Проблема
В таблице `users` отсутствует колонка `updated_at`, что вызывает ошибку при обновлении данных пользователя.

## Решение

### Шаг 1: Выполни SQL миграцию

Выполни файл `database/fix-missing-columns.sql` в своей базе данных:

```bash
psql 'YOUR_CONNECTION_STRING' -f database/fix-missing-columns.sql
```

Замени `YOUR_CONNECTION_STRING` на строку подключения из Render.com.

### Шаг 2: Проверь результат

После выполнения должно появиться:
```
NOTICE: Добавлена колонка updated_at в таблицу users
NOTICE: Добавлена колонка updated_at в таблицу addresses
NOTICE: Добавлена колонка updated_at в таблицу orders
NOTICE: Создан триггер update_users_updated_at
...
```

### Шаг 3: Проверь структуру таблиц

```bash
psql 'YOUR_CONNECTION_STRING' -c "\d users"
```

Должна быть колонка `updated_at`.

---

## Альтернатива: Пересоздать таблицы (если нет важных данных)

Если в базе нет важных данных, можно пересоздать таблицы:

```bash
# Удалить таблицы (ОСТОРОЖНО: удалит все данные!)
psql 'YOUR_CONNECTION_STRING' -c "DROP TABLE IF EXISTS order_items CASCADE; DROP TABLE IF EXISTS orders CASCADE; DROP TABLE IF EXISTS addresses CASCADE; DROP TABLE IF EXISTS users CASCADE;"

# Создать таблицы заново
psql 'YOUR_CONNECTION_STRING' -f database/schema.sql
```

---

## После исправления

После выполнения миграции:
1. Перезапусти сервис в Render.com (или подожди автоматического перезапуска)
2. Создай тестовый заказ
3. Проверь логи - ошибка должна исчезнуть

