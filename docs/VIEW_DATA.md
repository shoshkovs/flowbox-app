# 📊 Как просмотреть данные в базе Render.com

## ⚠️ Важно: Render.com не имеет встроенного Table Editor

Render.com предоставляет только:
- ✅ Логи базы данных
- ✅ Метрики (CPU, память, подключения)
- ✅ Строку подключения
- ❌ НЕТ встроенного просмотра таблиц

## 🔍 Варианты просмотра данных

### Вариант 1: Через psql (в терминале Mac)

```bash
# Подключись к базе
psql 'postgresql://flowbox_db_user:password@dpg-xxxxx-a.oregon-postgres.render.com/flowbox_db'

# Посмотри список таблиц
\dt

# Посмотри все пользователи
SELECT * FROM users;

# Посмотри все заказы
SELECT * FROM orders ORDER BY created_at DESC;

# Посмотри заказы с позициями
SELECT 
    o.id,
    o.total,
    o.address_string,
    o.created_at,
    json_agg(oi.*) as items
FROM orders o
LEFT JOIN order_items oi ON o.id = oi.order_id
GROUP BY o.id
ORDER BY o.created_at DESC;

# Выход из psql
\q
```

### Вариант 2: DBeaver (бесплатный, графический интерфейс)

1. **Скачай DBeaver**: https://dbeaver.io/download/
2. **Установи** (просто перетащи в Applications на Mac)
3. **Создай новое подключение**:
   - Выбери **PostgreSQL**
   - **Host**: `dpg-xxxxx-a.oregon-postgres.render.com` (из строки подключения)
   - **Port**: `5432`
   - **Database**: `flowbox_db`
   - **Username**: `flowbox_db_user`
   - **Password**: твой пароль из Render.com
4. **Подключись** и просматривай таблицы в графическом интерфейсе

### Вариант 3: TablePlus (платный, но есть бесплатная версия)

1. **Скачай TablePlus**: https://tableplus.com/
2. **Создай новое подключение** → PostgreSQL
3. **Введи данные** из строки подключения Render.com
4. **Просматривай** таблицы в удобном интерфейсе

### Вариант 4: pgAdmin (официальный клиент PostgreSQL)

1. **Скачай pgAdmin**: https://www.pgadmin.org/download/
2. **Установи** и создай подключение к базе Render.com
3. **Просматривай** данные через веб-интерфейс

---

## ✅ Быстрая проверка через psql

Выполни эти команды для проверки:

```bash
# 1. Подключись к базе (замени на свою строку подключения)
psql 'postgresql://flowbox_db_user:password@dpg-xxxxx-a.oregon-postgres.render.com/flowbox_db'

# 2. Проверь, что таблицы созданы
\dt

# Должны быть:
# - users
# - addresses
# - orders
# - order_items

# 3. Посмотри структуру таблицы users
\d users

# 4. Посчитай количество записей (должно быть 0, если еще нет данных)
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM addresses;
SELECT COUNT(*) FROM orders;

# 5. Выход
\q
```

---

## 🧪 Тест: Создай тестового пользователя

После того как MiniApp создаст пользователя, проверь:

```bash
psql 'YOUR_CONNECTION_STRING' -c "SELECT * FROM users;"
```

Должен появиться пользователь с твоим `telegram_id`.

---

## 📝 Полезные SQL запросы для проверки

```sql
-- Все пользователи с их адресами
SELECT 
    u.telegram_id,
    u.first_name,
    u.phone,
    COUNT(a.id) as address_count
FROM users u
LEFT JOIN addresses a ON u.id = a.user_id
GROUP BY u.id;

-- Все активные заказы
SELECT 
    o.id,
    o.total,
    o.address_string,
    o.delivery_date,
    o.delivery_time,
    o.created_at
FROM orders o
WHERE o.status = 'active'
ORDER BY o.created_at DESC;

-- Заказ с позициями
SELECT 
    o.id as order_id,
    o.total,
    o.address_string,
    oi.name as product_name,
    oi.quantity,
    oi.price
FROM orders o
JOIN order_items oi ON o.id = oi.order_id
WHERE o.id = 1;  -- замени на ID заказа
```

---

## 🎯 Рекомендация

Для удобного просмотра данных рекомендую **DBeaver** - он бесплатный, простой в использовании и имеет хороший графический интерфейс.

