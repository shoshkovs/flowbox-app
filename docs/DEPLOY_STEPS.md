# 🚀 Пошаговая инструкция деплоя на Render.com

## Шаг 1: Создание базы данных PostgreSQL

1. Зайдите на https://render.com и войдите в аккаунт
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name:** `flowbox-db`
   - **Database:** `flowbox` (или оставьте по умолчанию)
   - **User:** (автоматически)
   - **Region:** Выберите ближайший (например, Frankfurt)
   - **PostgreSQL Version:** 15 (или последняя)
   - **Plan:** Free (для начала)
4. Нажмите **"Create Database"**
5. Дождитесь создания (1-2 минуты)

### Применение схемы базы данных

1. В настройках БД нажмите **"Connect"**
2. Выберите **"External Connection"** или используйте **Render Shell**
3. Скопируйте **Connection String** (Internal Database URL)
4. Выполните SQL скрипты:

**Вариант A: Через psql (локально)**
```bash
# Установите PostgreSQL client (если нет)
# macOS: brew install postgresql
# Linux: sudo apt-get install postgresql-client

# Подключитесь к БД (используйте External Database URL)
psql "postgresql://user:password@host:port/database"

# Выполните скрипты
\i database/schema.sql
\i database/schema-admin-expansion.sql
```

**Вариант B: Через Render Shell**
1. В Render Dashboard → Database → **"Shell"**
2. Скопируйте SQL из `database/schema.sql`
3. Вставьте и выполните
4. Повторите для `database/schema-admin-expansion.sql`

**Вариант C: Через веб-интерфейс (pgAdmin/DBeaver)**
1. Используйте External Database URL
2. Подключитесь через клиент
3. Выполните SQL скрипты

## Шаг 2: Подготовка кода

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Add React admin panel"
git push origin main
```

2. Проверьте наличие файлов:
   - ✅ `render.yaml` в корне проекта
   - ✅ `admin/package.json` с зависимостями
   - ✅ `admin/vite.config.js` настроен
   - ✅ `package.json` содержит скрипт `build:admin`

## Шаг 3: Создание Web Service на Render

### Вариант A: Через Blueprint (render.yaml)

1. В Render Dashboard нажмите **"New +"** → **"Blueprint"**
2. Подключите GitHub репозиторий
3. Render автоматически обнаружит `render.yaml`
4. Проверьте настройки и нажмите **"Apply"**

### Вариант B: Вручную через Web Service

1. **"New +"** → **"Web Service"**
2. Подключите GitHub репозиторий
3. Выберите репозиторий и ветку (обычно `main`)
4. Настройки:
   - **Name:** `flowbox-app`
   - **Environment:** `Node`
   - **Region:** Выберите тот же, что и БД
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)
   - **Build Command:** `npm install && npm run build:admin`
   - **Start Command:** `npm start`
   - **Plan:** Free (или Starter для production)

## Шаг 4: Настройка переменных окружения

В настройках Web Service → **Environment** добавьте:

### Обязательные переменные:

1. **BOT_TOKEN**
   - Значение: токен из @BotFather
   - Пример: `8488111859:AAHEXqwUXwA8up6YyDxz14AcjemwzCa2dbI`

2. **DATABASE_URL**
   - Значение: Internal Database URL из настроек PostgreSQL
   - Формат: `postgresql://user:password@host:port/database`
   - ⚠️ Используйте **Internal Database URL** (не External!)
   - Найти можно: Database → Connect → Internal Database URL

3. **ADMIN_PASSWORD** (опционально)
   - Значение: пароль для админ-панели
   - По умолчанию: `admin123`
   - Можно оставить пустым, если используете дефолтный

### Автоматические переменные:

- **WEBAPP_URL** - автоматически устанавливается Render
- **PORT** - автоматически устанавливается Render (3000)

## Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет сборку:
   - Установка зависимостей сервера
   - Установка зависимостей админ-панели
   - Сборка React приложения
   - Запуск сервера

3. Дождитесь завершения (3-5 минут)
4. Проверьте статус: должен быть зеленый ✅

## Шаг 6: Проверка работы

### Проверка логов:

В Render Dashboard → Logs должны быть:
```
✅ Подключение к базе данных установлено и протестировано
🚀 Сервер запущен на http://localhost:3000
🔐 Админка доступна по адресу: https://your-app.onrender.com/admin
```

### Проверка админ-панели:

1. Откройте: `https://your-app.onrender.com/admin`
2. Введите пароль: `admin123`
3. Должна открыться админ-панель

### Проверка данных из БД:

1. В админ-панели перейдите в **"Товары"**
2. Если БД пустая, добавьте тестовый товар через SQL:
```sql
INSERT INTO products (name, price, type, color, image_url, is_active)
VALUES ('Красные розы', 2500, 'roses', 'red', 'https://via.placeholder.com/300', true);
```

3. Обновите страницу - товар должен появиться

## Шаг 7: Настройка Telegram бота

1. Получите URL от Render (например: `https://flowbox-app.onrender.com`)
2. Откройте @BotFather в Telegram
3. Найдите вашего бота
4. Команды:
   - `/mybots` → выберите бота
   - **Bot Settings** → **Menu Button**
   - Вставьте URL: `https://your-app.onrender.com`
   - Сохраните

5. Обновите переменную `WEBAPP_URL` в Render (если нужно)

## 🐛 Решение проблем

### Проблема: "База данных не подключена"

**Решение:**
1. Проверьте, что `DATABASE_URL` установлен в Environment
2. Убедитесь, что используете **Internal Database URL**
3. Проверьте логи на ошибки подключения
4. Убедитесь, что БД создана и запущена

### Проблема: "Админ-панель не собирается"

**Решение:**
1. Проверьте логи сборки в Render
2. Убедитесь, что `admin/package.json` содержит все зависимости
3. Проверьте, что `admin/vite.config.js` настроен правильно
4. Убедитесь, что Build Command: `npm install && npm run build:admin`

### Проблема: "401 Unauthorized" в админ-панели

**Решение:**
1. Проверьте, что пароль правильный (`admin123` по умолчанию)
2. Убедитесь, что `ADMIN_PASSWORD` в bot.js совпадает с тем, что вводите
3. Проверьте Network tab в браузере - должен быть заголовок `Authorization: Bearer admin123`

### Проблема: "Данные не отображаются"

**Решение:**
1. Проверьте, что таблицы созданы в БД:
```sql
\dt  -- список таблиц
SELECT * FROM products LIMIT 5;
SELECT * FROM orders LIMIT 5;
```

2. Добавьте тестовые данные:
```sql
INSERT INTO products (name, price, type, color, is_active)
VALUES ('Тестовый товар', 1000, 'roses', 'red', true);
```

3. Проверьте API в Network tab браузера
4. Убедитесь, что запросы идут с правильным Authorization header

## 📊 Проверка подключения к БД

В Render Shell выполните:
```bash
psql $DATABASE_URL

# Проверка таблиц
\dt

# Проверка данных
SELECT COUNT(*) FROM products;
SELECT COUNT(*) FROM orders;
SELECT COUNT(*) FROM users;

# Выход
\q
```

## ✅ Финальный чеклист

- [ ] PostgreSQL база данных создана
- [ ] Схема БД применена (выполнены SQL скрипты)
- [ ] Web Service создан на Render
- [ ] Переменные окружения настроены:
  - [ ] `BOT_TOKEN`
  - [ ] `DATABASE_URL` (Internal URL)
  - [ ] `ADMIN_PASSWORD` (опционально)
- [ ] Build Command: `npm install && npm run build:admin`
- [ ] Деплой успешно завершен
- [ ] Админ-панель доступна: `https://your-app.onrender.com/admin`
- [ ] Вход работает (пароль: `admin123`)
- [ ] Данные из БД отображаются в админ-панели
- [ ] Telegram бот настроен с правильным URL

## 🎉 Готово!

Ваша админ-панель теперь работает на Render.com и подключена к базе данных!

**Полезные ссылки:**
- Render Dashboard: https://dashboard.render.com
- Логи: Render Dashboard → Your Service → Logs
- База данных: Render Dashboard → Your Database → Connect

