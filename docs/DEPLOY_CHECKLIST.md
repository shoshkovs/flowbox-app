# ✅ Чеклист деплоя на Render.com

## 📋 Перед деплоем

### 1. Подготовка базы данных
- [ ] Создана PostgreSQL база данных на Render.com
- [ ] Выполнен `database/schema.sql` для создания таблиц
- [ ] Выполнен `database/schema-admin-expansion.sql` для расширения схемы
- [ ] Скопирован `DATABASE_URL` из настроек БД

### 2. Подготовка кода
- [ ] Все изменения закоммичены в Git
- [ ] Код запушен в GitHub
- [ ] `render.yaml` обновлен с правильными командами
- [ ] `package.json` содержит скрипт `build:admin`

### 3. Настройка Render.com

#### Создание Web Service:
- [ ] "New +" → "Web Service"
- [ ] Подключен GitHub репозиторий
- [ ] Name: `flowbox-app`
- [ ] Environment: `Node`
- [ ] Build Command: `npm install && npm run build:admin`
- [ ] Start Command: `npm start`
- [ ] Plan: Free (или Starter)

#### Переменные окружения:
- [ ] `BOT_TOKEN` = токен из @BotFather
- [ ] `DATABASE_URL` = URL из PostgreSQL (Internal Database URL)
- [ ] `PORT` = 3000 (автоматически)
- [ ] `WEBAPP_URL` = автоматически (из Render)

## 🚀 После деплоя

### Проверка работы:
- [ ] Деплой завершился успешно (зеленый статус)
- [ ] В логах видно: `✅ Подключение к базе данных установлено`
- [ ] В логах видно: `🚀 Сервер запущен`
- [ ] Админ-панель доступна: `https://your-app.onrender.com/admin`
- [ ] Вход в админ-панель работает (пароль: `admin123`)

### Проверка данных:
- [ ] В админ-панели отображаются товары из БД
- [ ] Можно просматривать заказы
- [ ] Данные сохраняются при создании заказа
- [ ] API endpoints отвечают корректно

## 🔧 Быстрая команда для проверки

```bash
# Проверка подключения к БД (в Render Shell)
psql $DATABASE_URL -c "SELECT COUNT(*) FROM products;"
psql $DATABASE_URL -c "SELECT COUNT(*) FROM orders;"
```

## 📝 Добавление тестовых данных

Если БД пустая, добавьте тестовые данные:

```sql
-- Добавление тестового товара
INSERT INTO products (name, price, type, color, image_url, is_active)
VALUES ('Красные розы', 2500, 'roses', 'red', 'https://via.placeholder.com/300', true);

-- Проверка
SELECT * FROM products;
```

## 🐛 Если что-то не работает

1. **Проверьте логи** в Render Dashboard → Logs
2. **Проверьте переменные окружения** в Settings → Environment
3. **Проверьте подключение к БД:**
   ```bash
   # В Render Shell
   psql $DATABASE_URL
   \dt  # список таблиц
   ```
4. **Проверьте сборку админ-панели:**
   - В логах должна быть строка: `✓ built in ...`
   - Должна существовать папка `admin-build/`

## 📞 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- PostgreSQL на Render: https://dashboard.render.com/new/postgres
- Документация Render: https://render.com/docs

