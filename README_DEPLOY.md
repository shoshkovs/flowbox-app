# 🚀 Быстрый деплой на Render.com

## 📝 Краткая инструкция

### 1. Создайте PostgreSQL базу данных

1. Render Dashboard → **"New +"** → **"PostgreSQL"**
2. Name: `flowbox-db`, Plan: Free
3. После создания → **"Connect"** → скопируйте **Internal Database URL**
4. Выполните SQL скрипты из `database/schema.sql` и `database/schema-admin-expansion.sql`

### 2. Создайте Web Service

1. Render Dashboard → **"New +"** → **"Web Service"** (или используйте Blueprint с `render.yaml`)
2. Подключите GitHub репозиторий
3. Настройки:
   - **Build Command:** `npm install && npm run build:admin`
   - **Start Command:** `npm start`

### 3. Добавьте переменные окружения

В настройках Web Service → **Environment**:

- `BOT_TOKEN` = токен из @BotFather
- `DATABASE_URL` = Internal Database URL из PostgreSQL
- `ADMIN_PASSWORD` = `admin123` (или свой пароль)

### 4. Дождитесь деплоя

После успешного деплоя:
- Админ-панель: `https://your-app.onrender.com/admin`
- Пароль: `admin123`

## 📚 Подробные инструкции

- **Полная инструкция:** `DEPLOY_STEPS.md`
- **Чеклист:** `DEPLOY_CHECKLIST.md`
- **Решение проблем:** `RENDER_DEPLOY.md`

## ✅ Проверка

После деплоя проверьте:
1. Логи в Render Dashboard (должно быть "✅ Подключение к базе данных")
2. Админ-панель открывается и работает
3. Данные из БД отображаются

## 🔧 Локальная проверка перед деплоем

```bash
# 1. Соберите админ-панель
cd admin && npm run build && cd ..

# 2. Проверьте подключение к БД (если есть DATABASE_URL)
npm start

# 3. Откройте админ-панель
open http://localhost:3000/admin
```

