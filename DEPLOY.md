# 🚀 Инструкция по деплою FlowBox

## Вариант 1: Render.com (Рекомендуется - бесплатно, без ограничений)

### Шаг 1: Подготовка
1. Зарегистрируйтесь на https://render.com (можно через GitHub)
2. Убедитесь, что ваш проект загружен на GitHub

### Шаг 2: Деплой
1. Войдите в Render Dashboard
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** `flowbox-app`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free

### Шаг 3: Переменные окружения
Добавьте в разделе "Environment":
- `BOT_TOKEN` = `8488111859:AAHEXqwUXwA8up6YyDxz14AcjemwzCa2dbI`
- `PORT` = `3000`
- `WEBAPP_URL` = (оставьте пустым, Render автоматически создаст URL)

### Шаг 4: Получение URL
После деплоя Render выдаст URL вида: `https://flowbox-app.onrender.com`

### Шаг 5: Обновление .env
Обновите `WEBAPP_URL` в настройках Render на полученный URL

### Шаг 6: Настройка MiniApp
1. Откройте @BotFather
2. Найдите @FlowboxBot
3. Mini Apps → Menu Button
4. Вставьте URL от Render

---

## Вариант 2: Railway.app (Альтернатива)

1. Зарегистрируйтесь на https://railway.app
2. "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `PORT=3000`
5. Railway автоматически создаст URL
6. Используйте этот URL в настройках MiniApp

---

## Вариант 3: Vercel (Только frontend, бот отдельно)

Vercel лучше подходит для статических сайтов. Для полноценного бота используйте Render или Railway.

---

## Быстрый старт с Render

```bash
# 1. Загрузите проект на GitHub (если еще не загружен)
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/FlowBox.git
git push -u origin main

# 2. Перейдите на render.com и следуйте инструкциям выше
```

---

## После деплоя

1. ✅ Получите URL от Render (например: `https://flowbox-app.onrender.com`)
2. ✅ Обновите `WEBAPP_URL` в настройках Render
3. ✅ Настройте MiniApp в @BotFather с этим URL
4. ✅ Протестируйте: откройте @FlowboxBot → /start → "Открыть магазин"

---

**Готово! Ваш магазин будет доступен 24/7! 🎉**

