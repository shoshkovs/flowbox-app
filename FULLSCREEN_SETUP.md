# Настройка Fullscreen для Mini App

## Вариант 1: Direct Link (рекомендуется)

### Настройка в @BotFather:

1. Откройте @BotFather в Telegram
2. Выберите вашего бота
3. Перейдите в **Mini Apps** → **Direct Links**
4. Создайте новый Direct Link:
   - **Name**: `flowbox` (или любое другое имя)
   - **URL**: `https://your-app-url.com` (URL вашего Mini App)
   - **Main App**: Включите опцию "Main App" (это важно для fullscreen)

5. После создания вы получите ссылку вида:
   ```
   https://t.me/your_bot_username/flowbox?startapp=main
   ```

6. Отправьте эту ссылку пользователям в сообщении бота

### Преимущества:
- ✅ Открывает Mini App в режиме **Main App**
- ✅ Fullscreen работает корректно
- ✅ Можно использовать в любых сообщениях бота

---

## Вариант 2: Кнопка под сообщением

### Текущая реализация:

В команде `/start` уже настроена inline-кнопка "🛍️ Открыть магазин" с `web_app`.

### Как это работает:

```javascript
{
  text: '🛍️ Открыть магазин',
  web_app: { url: webAppUrl }
}
```

### Преимущества:
- ✅ Уже реализовано в коде
- ✅ Открывает Mini App в режиме Main App
- ✅ Fullscreen работает корректно
- ✅ Удобно для пользователей

---

## Важно: Разница между Menu Button и кнопкой/Direct Link

### Menu Button (плавающий мини-апп):
- Открывается как "плавающее" окно
- Почти всегда fullsize, но может не всегда уважать настройку fullscreen
- Настраивается в @BotFather → Mini Apps → Menu Button

### Кнопка в сообщении / Direct Link:
- Открывает Mini App в режиме **Main App**
- Fullscreen **реально уважает** настройку `tg.expand()`
- Рекомендуется для корректной работы fullscreen

---

## Проверка работы fullscreen

В файле `public/app.js` уже настроено:

```javascript
const tg = window.Telegram.WebApp;
tg.ready();
tg.expand(); // Включает fullscreen режим
```

Fullscreen будет работать корректно при открытии через:
1. ✅ Direct Link (с параметром `?startapp=main`)
2. ✅ Inline кнопку с `web_app` (кнопка "Открыть магазин")

---

## Пример использования Direct Link в сообщении бота

```javascript
await ctx.reply(
  '🌸 Добро пожаловать!\n\nОткройте магазин:',
  {
    reply_markup: {
      inline_keyboard: [
        [
          {
            text: '🛍️ Открыть магазин',
            url: 'https://t.me/your_bot_username/flowbox?startapp=main'
          }
        ]
      ]
    }
  }
);
```

Или просто отправьте ссылку напрямую:
```
https://t.me/your_bot_username/flowbox?startapp=main
```
