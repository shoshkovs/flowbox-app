# Решение проблем с Fullscreen

## Если Mini App не открывается в fullscreen:

### 1. Проверьте настройки Direct Link в @BotFather

**Важно:** В настройках Direct Link должна быть включена опция **"Main App"**.

Как проверить:
1. Откройте @BotFather
2. Выберите вашего бота
3. Перейдите в **Mini Apps** → **Direct Links**
4. Найдите ваш Direct Link (`flowbox_app`)
5. Убедитесь, что включена опция **"Main App"**

Если опция не включена:
- Отредактируйте Direct Link
- Включите **"Main App"**
- Сохраните изменения

### 2. Используйте правильную ссылку с параметром

**Обязательно** добавляйте параметр `?startapp=main`:
```
https://t.me/FlowboxBot/flowbox_app?startapp=main
```

Без параметра Mini App может открыться не в fullscreen.

### 3. Проверьте версию Telegram WebApp API

В коде добавлены множественные вызовы `tg.expand()`:
- В inline скрипте в `<head>` (самый ранний)
- В начале `app.js` (до `tg.ready()`)
- После `tg.ready()`
- При событиях `load` и `DOMContentLoaded`

### 4. Альтернативные способы открытия

#### Способ 1: Через кнопку web_app (уже реализовано)
```javascript
{
  text: '🛍️ Открыть магазин',
  web_app: { url: webAppUrl }
}
```

#### Способ 2: Через Direct Link с параметром
```
https://t.me/FlowboxBot/flowbox_app?startapp=main
```

### 5. Проверка в разных клиентах Telegram

Fullscreen может работать по-разному в:
- **Telegram Desktop** - может не поддерживать fullscreen
- **Telegram Mobile (iOS/Android)** - должен работать корректно
- **Telegram Web** - может иметь ограничения

**Рекомендация:** Тестируйте на мобильных устройствах (iOS/Android).

### 6. Отладка

Откройте консоль разработчика и проверьте:
```javascript
// Проверьте, что Telegram WebApp загружен
console.log(window.Telegram);
console.log(window.Telegram.WebApp);

// Проверьте версию
console.log(window.Telegram.WebApp.version);

// Попробуйте вызвать expand() вручную
window.Telegram.WebApp.expand();
```

### 7. Если ничего не помогает

Возможные причины:
1. **Direct Link не настроен как Main App** - проверьте в @BotFather
2. **Используется Menu Button** - он может не поддерживать fullscreen
3. **Старая версия Telegram** - обновите приложение
4. **Ограничения платформы** - некоторые клиенты не поддерживают fullscreen

### 8. Проверка через Bot API

Убедитесь, что в настройках бота через Bot API Mini App настроен как Main App:

```javascript
// Пример настройки через Bot API (если используете)
await bot.telegram.setChatMenuButton({
  menu_button: {
    type: 'web_app',
    text: 'Открыть магазин',
    web_app: {
      url: webAppUrl
    }
  }
});
```

## Текущая реализация в коде:

1. **Inline скрипт в `<head>`** - самый ранний вызов `expand()`
2. **В начале `app.js`** - до `tg.ready()`
3. **После `tg.ready()`** - с установкой `viewportStableHeight`
4. **При событиях загрузки** - для надежности

Если fullscreen все еще не работает, проблема скорее всего в настройках Direct Link в @BotFather (опция "Main App" не включена).
