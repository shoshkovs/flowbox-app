# 🚀 Быстрый старт FlowBox Admin Panel

## ✅ Проверка установки

1. **Убедитесь, что админ-панель собрана:**
```bash
cd admin
npm run build
```

Должна появиться папка `../admin-build/` с файлами.

2. **Запустите сервер:**
```bash
cd ..
npm start
```

## 🌐 Доступ к админ-панели

После запуска сервера откройте в браузере:
- **Админ-панель:** http://localhost:3000/admin
- **Пароль:** `admin123`

## 🔧 Разработка

Для разработки с hot-reload:
```bash
cd admin
npm run dev
```

Админ-панель будет доступна на: http://localhost:5173

## 📋 Структура проекта

```
FlowBox/
├── admin/              # React админ-панель (исходники)
│   ├── src/
│   │   ├── components/  # Компоненты страниц
│   │   ├── App.jsx      # Главный компонент
│   │   └── main.jsx     # Точка входа
│   └── package.json
├── admin-build/        # Собранная версия (создается после npm run build)
├── bot.js              # Express сервер
└── package.json        # Зависимости сервера
```

## ⚠️ Важно

- После изменений в админ-панели нужно пересобрать: `cd admin && npm run build`
- Express автоматически использует собранную версию из `admin-build/`
- Если сборка отсутствует, используется старая HTML версия

## 🐛 Решение проблем

**Ошибка "Could not resolve entry module":**
- Убедитесь, что находитесь в папке `admin` при запуске `npm run build`
- Проверьте наличие файла `admin/index.html`

**Админ-панель не загружается:**
- Проверьте, что папка `admin-build/` существует
- Убедитесь, что сервер запущен (`npm start`)
- Проверьте консоль браузера на ошибки

**Статические файлы не загружаются:**
- Убедитесь, что в `vite.config.js` указан `base: '/admin/'`
- Пересоберите проект: `cd admin && npm run build`

