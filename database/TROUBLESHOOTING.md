# 🔧 Диагностика проблем с базой данных

## Проблема: Данные не сохраняются в БД

### Шаг 1: Проверь логи в Render.com

1. Зайди в Render.com → твой веб-сервис (где запущен бот)
2. Перейди в раздел **"Logs"**
3. Ищи сообщения:
   - `✅ Подключение к базе данных установлено` - БД подключена
   - `⚠️ DATABASE_URL не установлен` - переменная не настроена
   - `❌ Ошибка подключения к БД` - проблема с подключением
   - `✅ Заказ создан в БД` - заказ сохранен
   - `❌ Ошибка createOrderInDb` - ошибка при сохранении

### Шаг 2: Проверь переменную DATABASE_URL

1. Зайди в Render.com → твой веб-сервис
2. Перейди в **Environment**
3. Убедись, что есть переменная `DATABASE_URL`
4. Значение должно быть строкой подключения из базы данных:
   ```
   postgresql://flowbox_db_user:password@dpg-xxxxx-a.oregon-postgres.render.com/flowbox_db
   ```

### Шаг 3: Проверь подключение к БД

Выполни в терминале:

```bash
# Замени на свою строку подключения
psql 'postgresql://flowbox_db_user:password@dpg-xxxxx-a.oregon-postgres.render.com/flowbox_db' -c "SELECT NOW();"
```

Если команда выполняется без ошибок - подключение работает.

### Шаг 4: Проверь, что таблицы существуют

```bash
psql 'YOUR_CONNECTION_STRING' -c "\dt"
```

Должны быть таблицы: `users`, `addresses`, `orders`, `order_items`.

### Шаг 5: Проверь, что данные сохраняются

После создания заказа через MiniApp выполни:

```bash
# Проверь заказы
psql 'YOUR_CONNECTION_STRING' -c "SELECT id, total, address_string, created_at FROM orders ORDER BY created_at DESC LIMIT 5;"

# Проверь пользователей
psql 'YOUR_CONNECTION_STRING' -c "SELECT telegram_id, first_name, phone FROM users;"

# Проверь позиции заказов
psql 'YOUR_CONNECTION_STRING' -c "SELECT * FROM order_items LIMIT 10;"
```

---

## Частые ошибки

### Ошибка: "relation does not exist"
**Причина:** Таблицы не созданы  
**Решение:** Выполни SQL схему из `database/schema.sql`

### Ошибка: "password authentication failed"
**Причина:** Неправильный пароль в строке подключения  
**Решение:** Проверь строку подключения в Render.com

### Ошибка: "could not connect to server"
**Причина:** 
- Неправильный хост/порт
- База данных остановлена
- Используешь Internal URL вместо External (или наоборот)

**Решение:** 
- Проверь статус базы в Render.com (должна быть "Available")
- Используй Internal Database URL для подключения из Render
- Используй External Database URL для подключения с локального Mac

### Ошибка: "SSL connection required"
**Причина:** Render.com требует SSL подключение  
**Решение:** Код уже исправлен, убедись что используешь последнюю версию

### Данные не появляются после создания заказа
**Причина:** 
- `DATABASE_URL` не установлен в веб-сервисе
- Ошибка при сохранении (смотри логи)
- Заказ создается, но без `userId`

**Решение:**
1. Проверь логи в Render.com
2. Убедись, что `DATABASE_URL` установлен
3. Проверь, что пользователь создается в таблице `users` при первом входе

---

## Тестовый скрипт для проверки

Создай файл `test-db.js`:

```javascript
const { Pool } = require('pg');
require('dotenv').config();

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: { rejectUnauthorized: false }
});

async function test() {
  try {
    console.log('🔌 Тестирование подключения...');
    const res = await pool.query('SELECT NOW()');
    console.log('✅ Подключение работает:', res.rows[0].now);
    
    console.log('\n📋 Таблицы:');
    const tables = await pool.query(`
      SELECT table_name 
      FROM information_schema.tables 
      WHERE table_schema = 'public'
    `);
    console.log(tables.rows.map(r => r.table_name).join(', '));
    
    console.log('\n👥 Пользователи:');
    const users = await pool.query('SELECT COUNT(*) FROM users');
    console.log('Количество:', users.rows[0].count);
    
    console.log('\n🛍️ Заказы:');
    const orders = await pool.query('SELECT COUNT(*) FROM orders');
    console.log('Количество:', orders.rows[0].count);
    
    await pool.end();
  } catch (error) {
    console.error('❌ Ошибка:', error.message);
    process.exit(1);
  }
}

test();
```

Запусти:
```bash
node test-db.js
```

---

## Проверка через Render.com Dashboard

1. Зайди в Render.com → база данных `flowbox_db`
2. Перейди в **"Logs"** - там будут логи подключений
3. Перейди в **"Metrics"** - там будет статистика подключений

---

## Если ничего не помогает

1. Перезапусти веб-сервис в Render.com
2. Проверь, что код задеплоен (последний коммит в GitHub)
3. Убедись, что `DATABASE_URL` правильно скопирован (без лишних пробелов, кавычек)
4. Проверь, что база данных в статусе "Available"

