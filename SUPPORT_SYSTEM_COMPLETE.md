# 💬 Полная документация системы поддержки FlowBox

**Дата создания:** 2025-12-09  
**Версия:** 2.0

---

## 📋 Содержание

1. [Описание системы](#описание-системы)
2. [Настройка](#настройка)
3. [UX Flow](#ux-flow)
4. [Технические детали](#технические-детали)
5. [Диагностика и решение проблем](#диагностика-и-решение-проблем)
6. [Код и функции](#код-и-функции)

---

## Описание системы

Система поддержки построена на основе **Telegram Forum Topics**. Каждый пользователь получает свой отдельный топик в форум-чате поддержки. Все сообщения пользователя автоматически попадают в его топик, менеджеры отвечают в топике, и ответы автоматически отправляются пользователю.

### Ключевые особенности:

- ✅ Каждый пользователь имеет **один уникальный топик**
- ✅ Связь пользователь ↔ топик хранится в БД (не теряется при перезапуске)
- ✅ Поддерживаются все типы сообщений (текст, фото, видео, документы, голосовые)
- ✅ Подтверждающее сообщение пользователю **НЕ отправляется** (по требованию)
- ✅ Автоматическое создание топика при первом обращении

---

## Настройка

### Шаг 1: Создание форум-чата поддержки

⚠️ **КРИТИЧНО:** Чат поддержки **ОБЯЗАТЕЛЬНО** должен быть форумом!

1. Создай **супергруппу** в Telegram (например, "FlowBox Support")
2. **Включи режим "Topics" (Форум):**
   - Открой настройки группы
   - `Профиль чата → Edit → Topics/Форум → Enable`
3. Добавь туда бота (например, `@FlowboxBot`)
4. Дай боту права **администратора** с правами:
   - ✅ **Manage Topics** (Управление темами) - **ОБЯЗАТЕЛЬНО!**
   - ✅ **Send messages** (Отправка сообщений)
   - ✅ **Delete messages** (желательно)
5. Добавь менеджеров в эту группу

### Шаг 2: Получение ID чата поддержки

**Способ 1: Через бота (рекомендуется)**
1. Отправь любое сообщение в группу поддержки
2. В логах бота найди строку с `ctx.chat.id` - это будет отрицательное число (например, `-1001234567890`)
3. Это и есть `SUPPORT_CHAT_ID`

**Способ 2: Через @userinfobot**
1. Добавь `@userinfobot` в группу поддержки
2. Отправь команду `/start`
3. Бот покажет ID группы (будет отрицательное число)

**Способ 3: Через Telegram API**
1. Отправь сообщение в группу
2. Открой `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
3. Найди `"chat":{"id":-1001234567890}` - это и есть ID чата

### Шаг 3: Настройка переменной окружения

Добавь в `.env` или в настройки Render.com:

```env
SUPPORT_CHAT_ID=-1001234567890
```

⚠️ **Важно:** 
- ID должен быть числом (может быть отрицательным для групп)
- Не используй кавычки вокруг числа
- Убедись, что нет лишних пробелов

### Шаг 4: Перезапуск бота

После добавления переменной окружения перезапусти бота. В логах должно появиться:

```
🔍 SUPPORT_CHAT_ID (raw): "-1001234567890" (type: string)
🔍 SUPPORT_CHAT_ID (parsed): -1001234567890 (type: number)
✅ Чат поддержки настроен: -1001234567890
💡 Убедитесь, что:
   1. Чат является форумом (Topics включены)
   2. Бот имеет права администратора
   3. У бота есть права "Manage Topics" и "Send messages"
```

Если переменная не установлена:

```
⚠️  SUPPORT_CHAT_ID не установлен. Система поддержки будет недоступна.
```

---

## UX Flow

### Для пользователей:

1. Пользователь пишет боту лично:
   - Команда `/support` или просто любое сообщение
   - Может отправить текст, фото, документ, видео, голосовое сообщение

2. Бот автоматически:
   - Проверяет, есть ли уже топик для этого пользователя
   - Если нет - создает новый топик
   - Если есть - использует существующий

3. Сообщение отправляется в топик:
   - Если это первое сообщение - отправляется шапка с информацией о пользователе
   - Ниже отправляется само сообщение пользователя

4. **НЕ отправляется** подтверждающее сообщение пользователю (по требованию)

### Для менеджеров:

1. В форум-чате поддержки для каждого пользователя создается отдельный топик:
   - **Название топика:** `Тикет {user_id} ({username})`
   - Пример: `Тикет 1059138125 (@shoshkovs)`

2. В топике отображается шапка с информацией о пользователе (только для первого сообщения):
   ```
   👤 Новый запрос в поддержку
   
   👤 Имя: Саша
   🆔 ID: 1059138125
   📝 Username: @shoshkovs
   📱 Телефон: +7 (965) 080-08-13
   📧 Email: alexansho@gmail.com
   ```

3. Ниже отправляется само сообщение пользователя (текст или медиа)

4. Менеджер пишет ответ прямо в топике:
   - Не обязательно делать Reply
   - Может отправить текст, фото, документ, видео, голосовое сообщение

5. Бот автоматически:
   - Определяет пользователя по `message_thread_id` из БД
   - Отправляет ответ пользователю
   - Подтверждает менеджеру: "✅ Ответ отправлен пользователю"

---

## Технические детали

### База данных

**Таблица `support_topics`:**

```sql
CREATE TABLE IF NOT EXISTS support_topics (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,              -- ID пользователя Telegram
  message_thread_id INTEGER NOT NULL,   -- ID топика (thread_id из Telegram)
  topic_name TEXT,                       -- Название топика
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id),                       -- Один топик на пользователя
  UNIQUE(message_thread_id)              -- Один пользователь на топик
);
```

**Индексы:**
```sql
CREATE INDEX idx_support_topics_user_id ON support_topics(user_id);
CREATE INDEX idx_support_topics_message_thread_id ON support_topics(message_thread_id);
```

**Автоматическая миграция:**
- Таблица создается автоматически при первом запуске
- Колонки `message_thread_id`, `topic_name`, `updated_at` добавляются автоматически, если их нет
- Используется `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` для безопасного обновления схемы

### Telegram API методы

**`createForumTopic`:**
- Создает новый форум-топик в чате
- Требования:
  - Чат должен быть форумом
  - Бот должен быть администратором с правами "Manage Topics"
- Возвращает объект с `message_thread_id`

**`sendMessage` с `message_thread_id`:**
- Отправляет сообщение в конкретный топик
- Обязательный параметр: `message_thread_id`

**`copyMessage` с `message_thread_id`:**
- Копирует медиа-сообщение в конкретный топик
- Используется для фото, видео, документов, голосовых

### Основные функции (`bot.js`)

#### `getOrCreateSupportTopic(userId, userName, username)`

**Назначение:** Получить существующий топик для пользователя или создать новый.

**Логика:**
1. Проверяет, есть ли уже топик для этого `user_id` в БД
2. Если есть - возвращает `message_thread_id`
3. Если нет:
   - Создает новый топик через `createForumTopic`
   - Сохраняет связь `user_id ↔ message_thread_id` в БД
   - Возвращает `message_thread_id`

**Логирование:**
- `[support] 🔍 getOrCreateSupportTopic для userId=...`
- `[support] 📊 Поиск существующего топика...`
- `[support] ✅ Найден существующий топик...` или `[support] 🆕 Создаём новый топик...`
- `[support] ✅ Топик создан в Telegram: threadId=...`
- `[support] 💾 INSERT в support_topics выполнен:`
- `[support] 🔍 Проверка после INSERT...`

**Обработка ошибок:**
- Логирует все ошибки с деталями
- При ошибке создания топика возвращает `null`
- Проверяет наличие записи в БД после INSERT

#### `getUserIdByThreadId(messageThreadId)`

**Назначение:** Получить `user_id` по `message_thread_id`.

**Логика:**
1. Выполняет `SELECT user_id FROM support_topics WHERE message_thread_id = ?`
2. Если запись найдена - возвращает `user_id`
3. Если не найдена - возвращает `null`

**Логирование:**
- `[support] 🔍 getUserIdByThreadId ищем userId для threadId=...`
- `[support] 📊 Результат поиска по threadId=...:`
- `[support] ✅ Нашли user_id ... по threadId ...` или `[support] ❌ Не нашли user_id...`
- Если не найдено - показывает все записи в таблице для диагностики

#### Обработчик личных сообщений

**Триггер:** `bot.on('message')` когда `chat.type === 'private'`

**Логика:**
1. Пропускает команды (сообщения, начинающиеся с `/`)
2. Пропускает пустые сообщения
3. Вызывает `getOrCreateSupportTopic(userId, userName, username)`
4. Если топик создан/найден:
   - Получает информацию о пользователе из БД (телефон, email)
   - Определяет, нужно ли отправлять шапку (проверяет `created_at` топика)
   - Отправляет шапку в топик (если нужно)
   - Отправляет сообщение пользователя в топик
5. **НЕ отправляет** подтверждающее сообщение пользователю

#### Обработчик ответов менеджеров

**Триггер:** `bot.on('message')` когда `chat.id === SUPPORT_CHAT_ID`

**Логика:**
1. Проверяет, что сообщение в топике (`message_thread_id` присутствует)
2. Если сообщение не в топике - игнорирует
3. Вызывает `getUserIdByThreadId(messageThreadId)`
4. Если `userId` найден:
   - Вызывает `sendManagerReplyToUser(ctx, userId)`
5. Если `userId` не найден:
   - Отправляет ошибку менеджеру: "⚠️ Не удалось определить пользователя для этого топика. Нет записи в support_topics."

#### `sendManagerReplyToUser(ctx, userId)`

**Назначение:** Отправить ответ менеджера пользователю.

**Логика:**
1. Извлекает текст/медиа из сообщения менеджера
2. Пропускает команды (сообщения, начинающиеся с `/`)
3. Если есть медиа - копирует через `copyMessage`
4. Если есть только текст - отправляет через `sendMessage`
5. Подтверждает менеджеру: "✅ Ответ отправлен пользователю"

---

## Диагностика и решение проблем

### Проблема: Каждое новое сообщение создает новый топик

**Симптомы:**
- Пользователь отправляет несколько сообщений боту
- В чате поддержки создается несколько топиков для одного пользователя
- В логах: каждый раз "Создаём новый топик" вместо "Найден существующий топик"

**Возможные причины:**

1. **Записи не сохраняются в БД**
   - Проверить логи: есть ли ошибки при INSERT
   - Проверить, что INSERT выполняется: искать `[support] 💾 INSERT в support_topics выполнен:`
   - Проверить БД: `SELECT * FROM support_topics WHERE user_id = ?`

2. **Неправильный тип данных `user_id`**
   - Проверить, что в таблице `user_id` имеет тип `BIGINT`
   - Проверить, что в запросах используется `$1::bigint`

3. **Транзакция откатывается**
   - Проверить, нет ли `ROLLBACK` после INSERT
   - Убедиться, что используется один и тот же `client` из пула

4. **Разные БД**
   - Убедиться, что `DATABASE_URL` одинаковый везде
   - Проверить, что используется один `pool` подключений

**Решение:**

1. Добавить детальное логирование в `getOrCreateSupportTopic`:
   ```js
   console.log(`[support] 📊 Поиск существующего топика для userId=${userId}:`, existing.rows);
   console.log(`[support] 💾 INSERT в support_topics выполнен:`, insertRes.rows);
   console.log(`[support] 🔍 Проверка после INSERT:`, verifyResult.rows);
   ```

2. Проверить БД вручную:
   ```sql
   SELECT * FROM support_topics;
   SELECT * FROM support_topics WHERE user_id = 1059138125;
   ```

3. Проверить структуру таблицы:
   ```sql
   \d+ support_topics
   ```

### Проблема: "Не удалось определить пользователя для этого топика"

**Симптомы:**
- Менеджер пишет ответ в топике
- Бот отвечает: "⚠️ Не удалось определить пользователя для этого топика. Нет записи в support_topics."

**Возможные причины:**

1. **Запись не была создана при создании топика**
   - Проверить логи создания топика
   - Проверить, была ли ошибка при INSERT

2. **Неправильный `message_thread_id`**
   - Убедиться, что используется правильный ID из `createForumTopic`
   - Проверить, что ID сохраняется в БД

3. **Тип данных не совпадает**
   - Проверить, что в БД `message_thread_id` имеет тип `INTEGER`
   - Проверить, что в запросах используется `$1::integer`

**Решение:**

1. Проверить записи в БД:
   ```sql
   SELECT * FROM support_topics WHERE message_thread_id = 40;
   ```

2. Добавить логирование в `getUserIdByThreadId`:
   ```js
   console.log(`[support] 📊 Результат поиска по threadId=${messageThreadId}:`, result.rows);
   ```

3. Проверить, что INSERT действительно выполнился:
   - Искать в логах `[support] 💾 INSERT в support_topics выполнен:`
   - Искать `[support] ✅ Проверка успешна: связь user_id=... ↔ thread_id=... действительно сохранена в БД`

### Проблема: Ошибка "CHAT_FORUM_REQUIRED"

**Причина:** Чат не является форумом (Topics не включены)

**Решение:**
1. Открой настройки группы
2. `Профиль чата → Edit → Topics/Форум → Enable`
3. Перезапусти бота

### Проблема: Ошибка "CHAT_ADMIN_REQUIRED"

**Причина:** Бот не является администратором или не имеет прав "Manage Topics"

**Решение:**
1. Убедись, что бот добавлен как администратор
2. Проверь права бота:
   - Открой настройки группы
   - `Администраторы → @FlowboxBot → Права`
   - Убедись, что включено **"Manage Topics"**
3. Перезапусти бота

### Проблема: Ошибка "column 'message_thread_id' does not exist"

**Причина:** Таблица создана, но колонка отсутствует

**Решение:** 
- Миграция автоматически создаст колонку при следующем запуске
- Проверь логи на наличие: `🔄 Добавляем колонку message_thread_id...`

### Проблема: Ошибка "column 'topic_name' does not exist"

**Причина:** Таблица создана, но колонка отсутствует

**Решение:**
- Миграция автоматически создаст колонку при следующем запуске
- Проверь логи на наличие: `🔄 Добавляем колонку topic_name...`

### Проблема: Ошибка "column 'updated_at' does not exist"

**Причина:** Таблица создана, но колонка отсутствует

**Решение:**
- Миграция автоматически создаст колонку при следующем запуске
- Проверь логи на наличие: `🔄 Добавляем колонку updated_at...`

---

## Код и функции

### Файлы

**`bot.js`:**
- `getOrCreateSupportTopic(userId, userName, username)` - строки ~6914-7070
- `getUserIdByThreadId(messageThreadId)` - строки ~7072-7105
- `bot.on('message')` обработчик личных сообщений - строки ~7241-7350
- `bot.on('message')` обработчик ответов менеджеров - строки ~7352-7380
- `sendManagerReplyToUser(ctx, userId)` - строки ~7382-7430

**Миграции:**
- Строки ~704-811: миграция таблицы `support_topics`

### Переменные окружения

```env
SUPPORT_CHAT_ID=-1001234567890  # ID форум-чата поддержки (обязательно)
```

### Логика работы

**Создание/получение топика:**

```javascript
// 1. Проверяем существующий топик
const existing = await client.query(
  'SELECT message_thread_id, topic_name FROM support_topics WHERE user_id = $1::bigint',
  [userId]
);

if (existing.rows.length > 0) {
  return existing.rows[0].message_thread_id; // Используем существующий
}

// 2. Создаем новый топик
const topic = await bot.telegram.callApi('createForumTopic', {
  chat_id: SUPPORT_CHAT_ID,
  name: `Тикет ${userId} (${username || userName})`
});

const messageThreadId = topic.message_thread_id;

// 3. Сохраняем связь в БД
await client.query(
  `INSERT INTO support_topics (user_id, message_thread_id, topic_name, updated_at)
   VALUES ($1::bigint, $2::integer, $3::text, now())
   ON CONFLICT (user_id) DO UPDATE SET
     message_thread_id = EXCLUDED.message_thread_id,
     topic_name = EXCLUDED.topic_name,
     updated_at = now()
   RETURNING id, user_id, message_thread_id`,
  [userId, messageThreadId, topicName]
);

// 4. Проверяем, что запись сохранена
const verify = await client.query(
  'SELECT user_id, message_thread_id FROM support_topics WHERE user_id = $1::bigint',
  [userId]
);
```

**Получение userId по threadId:**

```javascript
const result = await client.query(
  'SELECT user_id, topic_name FROM support_topics WHERE message_thread_id = $1::integer',
  [messageThreadId]
);

if (result.rows.length === 0) {
  return null; // Топик не найден
}

return result.rows[0].user_id;
```

---

## Чек-лист проверки

### Настройка

- [ ] Чат создан как супергруппа
- [ ] Topics включены в настройках чата
- [ ] Бот добавлен в группу как администратор
- [ ] У бота есть права "Manage Topics"
- [ ] У бота есть права "Send messages"
- [ ] `SUPPORT_CHAT_ID` установлен в переменных окружения
- [ ] Бот перезапущен после настройки

### Функциональность

- [ ] Пользователь может написать боту в личку
- [ ] Создается топик в чате поддержки
- [ ] Топик имеет правильное название: "Тикет {user_id} ({username})"
- [ ] В топике отображается шапка с информацией о пользователе
- [ ] Сообщение пользователя появляется в топике
- [ ] Подтверждающее сообщение пользователю **НЕ отправляется**
- [ ] Менеджер может написать ответ в топике
- [ ] Ответ менеджера отправляется пользователю
- [ ] Менеджер получает подтверждение: "✅ Ответ отправлен пользователю"
- [ ] Второе сообщение того же пользователя попадает в тот же топик (не создается новый)

### База данных

- [ ] Таблица `support_topics` создана
- [ ] Колонки `user_id`, `message_thread_id`, `topic_name`, `updated_at` присутствуют
- [ ] При создании топика запись появляется в БД
- [ ] Запись содержит правильные `user_id` и `message_thread_id`
- [ ] Поиск по `user_id` находит запись
- [ ] Поиск по `message_thread_id` находит запись

### Логирование

- [ ] В логах видно создание топика
- [ ] В логах видно INSERT в support_topics
- [ ] В логах видно проверку после INSERT
- [ ] При ответе менеджера видно поиск userId по threadId

---

## Примеры логов

### Успешное создание топика:

```
[support] 🔍 getOrCreateSupportTopic для userId=1059138125, userName=Саша, username=@shoshkovs
[support] 📊 Поиск существующего топика для userId=1059138125: []
[support] 🆕 Создаём новый топик для userId=1059138125, название="Тикет 1059138125 (@shoshkovs)"
[support] ✅ Топик создан в Telegram: threadId=40 для userId=1059138125
[support] 💾 INSERT в support_topics выполнен: [ { id: 1, user_id: '1059138125', message_thread_id: 40 } ]
[support] ✅ Связь сохранена: id=1, user_id=1059138125, thread_id=40
[support] 🔍 Проверка после INSERT для userId=1059138125: [ { user_id: '1059138125', message_thread_id: 40 } ]
[support] ✅ Проверка успешна: связь user_id=1059138125 ↔ thread_id=40 действительно сохранена в БД
```

### Использование существующего топика:

```
[support] 🔍 getOrCreateSupportTopic для userId=1059138125, userName=Саша, username=@shoshkovs
[support] 📊 Поиск существующего топика для userId=1059138125: [ { message_thread_id: 40, topic_name: 'Тикет 1059138125 (@shoshkovs)' } ]
[support] ✅ Найден существующий топик: threadId=40 для userId=1059138125
```

### Успешный ответ менеджера:

```
[support] 📨 Обработка ответа менеджера в топике 40
[support] 🔍 getUserIdByThreadId ищем userId для threadId=40
[support] 📊 Результат поиска по threadId=40: [ { user_id: '1059138125', topic_name: 'Тикет 1059138125 (@shoshkovs)' } ]
[support] ✅ Нашли user_id 1059138125 по threadId 40
[support] ✅ Отправляем ответ пользователю 1059138125
📥 Ответ менеджера отправлен пользователю 1059138125
```

### Ошибка: топик не найден:

```
[support] 📨 Обработка ответа менеджера в топике 40
[support] 🔍 getUserIdByThreadId ищем userId для threadId=40
[support] 📊 Результат поиска по threadId=40: []
[support] ❌ Не нашли user_id по threadId 40 в БД
[support] 📋 Всего записей в support_topics: []
```

---

## Важные замечания

1. **Чат ОБЯЗАТЕЛЬНО должен быть форумом** - без этого система не будет работать
2. **Бот должен иметь права "Manage Topics"** - без этого не получится создавать топики
3. **Каждый пользователь получает один топик** - все его сообщения идут в один топик
4. **Подтверждающее сообщение пользователю НЕ отправляется** (по требованию)
5. **Связь хранится в БД** - не теряется при перезапуске бота
6. **Логирование максимально подробное** - помогает быстро найти проблемы

---

**Дата обновления:** 2025-12-09  
**Версия документа:** 2.0  
**Статус:** Актуально
