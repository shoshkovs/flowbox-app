# 💬 Настройка системы поддержки "Живой чат"

Система поддержки работает по принципу: **пользователь пишет боту → сообщение пересылается в чат поддержки → менеджер отвечает reply → ответ отправляется пользователю**.

## 🚀 Быстрая настройка

### 1. Создание чата поддержки

1. Создай **группу** или **супергруппу** в Telegram (например, "FlowBox Support")
2. Добавь туда бота (`@FlowboxBot` или твой бот)
3. Дай боту права **администратора** (чтобы он мог читать все сообщения)
4. Добавь менеджеров в эту группу

### 2. Получение ID чата поддержки

**Способ 1: Через бота**
1. Отправь любое сообщение в группу поддержки
2. В логах бота найди строку с `ctx.chat.id` - это будет отрицательное число (например, `-1001234567890`)
3. Это и есть `SUPPORT_CHAT_ID`

**Способ 2: Через @userinfobot**
1. Добавь `@userinfobot` в группу поддержки
2. Отправь команду `/start`
3. Бот покажет ID группы (будет отрицательное число)

**Способ 3: Через API**
1. Отправь сообщение в группу
2. Открой `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
3. Найди `"chat":{"id":-1001234567890}` - это и есть ID чата

### 3. Настройка переменной окружения

Добавь в `.env` или в настройки Render.com:

```env
SUPPORT_CHAT_ID=-1001234567890
```

⚠️ **Важно:** ID должен быть числом (может быть отрицательным для групп).

### 4. Перезапуск бота

После добавления переменной окружения перезапусти бота. В логах должно появиться:

```
✅ Чат поддержки настроен: -1001234567890
```

Если переменная не установлена, будет предупреждение:

```
⚠️  SUPPORT_CHAT_ID не установлен. Система поддержки будет недоступна.
```

## 📋 Как это работает

### Для пользователей:

1. Пользователь пишет боту лично (команда `/support` или просто сообщение)
2. Бот отвечает: "Ваше сообщение передано в поддержку 🌸 Менеджер ответит здесь же."
3. Сообщение автоматически пересылается в чат поддержки с информацией о пользователе

### Для менеджеров:

1. В чате поддержки появляется сообщение с информацией о пользователе:
   ```
   👤 Новый запрос в поддержку
   
   👤 Имя: Иван Иванов
   🆔 ID: 123456789
   📝 Username: @ivan
   📱 Телефон: +79991234567
   📧 Email: ivan@example.com
   ```
2. Ниже пересылается само сообщение пользователя
3. Менеджер нажимает **Reply** на сообщении пользователя и пишет ответ
4. Бот автоматически отправляет ответ пользователю
5. Менеджер получает подтверждение: "✅ Ответ отправлен пользователю"

## 🔧 Технические детали

### База данных

Система использует таблицу `support_messages` для хранения связей:

```sql
CREATE TABLE support_messages (
  id SERIAL PRIMARY KEY,
  support_msg_id BIGINT NOT NULL,  -- ID сообщения в чате поддержки
  user_id BIGINT NOT NULL,          -- ID пользователя
  user_msg_id BIGINT,               -- ID оригинального сообщения пользователя
  manager_id BIGINT,                -- ID менеджера (опционально)
  manager_msg_id BIGINT,            -- ID ответа менеджера (опционально)
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(support_msg_id)
);
```

Таблица создается автоматически при первом запуске бота.

### Поддерживаемые типы сообщений

- ✅ Текст
- ✅ Фото
- ✅ Документы
- ✅ Видео
- ✅ Голосовые сообщения

### Безопасность

- Только сообщения с `reply_to_message` обрабатываются как ответы менеджеров
- Связь между сообщениями хранится в БД (не теряется при перезапуске)
- Fallback: если связь не найдена в БД, система пытается извлечь `user_id` из текста сообщения

## 🐛 Устранение проблем

### Бот не пересылает сообщения в чат поддержки

1. Проверь, что `SUPPORT_CHAT_ID` установлен правильно
2. Убедись, что бот добавлен в группу и имеет права администратора
3. Проверь логи бота на наличие ошибок

### Менеджер не может ответить пользователю

1. Убедись, что менеджер делает **Reply** на сообщение пользователя (не на шапку)
2. Проверь, что сообщение содержит текст или медиа
3. Убедись, что пользователь не заблокировал бота

### Сообщения теряются после перезапуска

Это нормально - связи хранятся в БД и восстанавливаются автоматически. Если проблема сохраняется:

1. Проверь подключение к БД
2. Убедись, что таблица `support_messages` создана
3. Проверь логи на ошибки БД

## 📝 Примеры использования

### Команда /support

Пользователь отправляет `/support` → бот объясняет, что можно писать → пользователь пишет вопрос → сообщение пересылается в чат поддержки.

### Прямое сообщение

Пользователь просто пишет боту → сообщение автоматически пересылается в чат поддержки (если это не команда).

### Ответ менеджера

Менеджер делает Reply на сообщении пользователя в чате поддержки → бот отправляет ответ пользователю → менеджер получает подтверждение.

## 🎯 Дополнительные возможности (будущие улучшения)

- [ ] Команды `/take` и `/done` для управления диалогами
- [ ] Статусы диалогов (открыт, в работе, закрыт)
- [ ] Уведомления о новых сообщениях
- [ ] История диалогов
- [ ] Статистика ответов

---

**Готово!** Система поддержки настроена и готова к работе. 🎉
