# 💬 Настройка системы поддержки "Живой чат" (Форум-топики)

Система поддержки работает по принципу: **пользователь пишет боту → создается/используется форум-топик для пользователя → сообщение отправляется в топик → менеджер отвечает в топике → ответ отправляется пользователю**.

## 🚀 Быстрая настройка

### 1. Создание форум-чата поддержки

⚠️ **ВАЖНО:** Чат поддержки должен быть **форумом** (супергруппой с включенными Topics)!

1. Создай **супергруппу** в Telegram (например, "FlowBox Support")
2. **Включи режим "Topics" (Форум):**
   - Открой настройки группы
   - `Профиль чата → Edit → Topics/Форум → Enable`
3. Добавь туда бота (`@FlowboxBot` или твой бот)
4. Дай боту права **администратора** с правами:
   - ✅ **Manage Topics** (Управление темами) - **ОБЯЗАТЕЛЬНО!**
   - ✅ **Send messages** (Отправка сообщений)
   - ✅ **Delete messages** (желательно)
5. Добавь менеджеров в эту группу

### 2. Получение ID чата поддержки

**Способ 1: Через бота**
1. Отправь любое сообщение в группу поддержки
2. В логах бота найди строку с `ctx.chat.id` - это будет отрицательное число (например, `-1001234567890`)
3. Это и есть `SUPPORT_CHAT_ID`

**Способ 2: Через @userinfobot**
1. Добавь `@userinfobot` в группу поддержки
2. Отправь команду `/start`
3. Бот покажет ID группы (будет отрицательное число)

**Способ 3: Через API**
1. Отправь сообщение в группу
2. Открой `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
3. Найди `"chat":{"id":-1001234567890}` - это и есть ID чата

### 3. Настройка переменной окружения

Добавь в `.env` или в настройки Render.com:

```env
SUPPORT_CHAT_ID=-1001234567890
```

⚠️ **Важно:** ID должен быть числом (может быть отрицательным для групп).

### 4. Перезапуск бота

После добавления переменной окружения перезапусти бота. В логах должно появиться:

```
✅ Чат поддержки настроен: -1001234567890
💡 Убедитесь, что:
   1. Чат является форумом (Topics включены)
   2. Бот имеет права администратора
   3. У бота есть права "Manage Topics" и "Send messages"
```

Если переменная не установлена, будет предупреждение:

```
⚠️  SUPPORT_CHAT_ID не установлен. Система поддержки будет недоступна.
```

## 📋 Как это работает

### Для пользователей:

1. Пользователь пишет боту лично (команда `/support` или просто сообщение)
2. Бот автоматически создает или находит форум-топик для этого пользователя
3. Сообщение отправляется в топик с информацией о пользователе
4. **НЕ отправляется подтверждающее сообщение** (по требованию)

### Для менеджеров:

1. В форум-чате поддержки для каждого пользователя создается отдельный топик:
   - Название топика: `Тикет {user_id} ({username})`
   - В топике появляется шапка с информацией о пользователе:
     ```
     👤 Новый запрос в поддержку
     
     👤 Имя: Иван Иванов
     🆔 ID: 123456789
     📝 Username: @ivan
     📱 Телефон: +79991234567
     📧 Email: ivan@example.com
     ```
2. Ниже отправляется само сообщение пользователя
3. Менеджер пишет ответ прямо в топике (не обязательно делать Reply)
4. Бот автоматически отправляет ответ пользователю
5. Менеджер получает подтверждение: "✅ Ответ отправлен пользователю"

## 🔧 Технические детали

### База данных

Система использует таблицу `support_topics` для хранения связей пользователей с топиками:

```sql
CREATE TABLE support_topics (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,              -- ID пользователя
  message_thread_id INTEGER NOT NULL,   -- ID топика (thread_id)
  topic_name TEXT,                       -- Название топика
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id),
  UNIQUE(message_thread_id)
);
```

Таблица создается автоматически при первом запуске бота.

### API методы

- **`createForumTopic`** - создает новый топик для пользователя
- **`sendMessage` с `message_thread_id`** - отправляет сообщение в конкретный топик
- **`copyMessage` с `message_thread_id`** - копирует медиа в топик

### Поддерживаемые типы сообщений

- ✅ Текст
- ✅ Фото
- ✅ Документы
- ✅ Видео
- ✅ Голосовые сообщения

### Безопасность

- Каждый пользователь имеет свой уникальный топик
- Связь между пользователем и топиком хранится в БД (не теряется при перезапуске)
- Fallback: если связь не найдена в БД, система пытается извлечь `user_id` из текста сообщения

## 🐛 Устранение проблем

### Ошибка: "CHAT_FORUM_REQUIRED"

**Причина:** Чат не является форумом (Topics не включены)

**Решение:**
1. Открой настройки группы
2. `Профиль чата → Edit → Topics/Форум → Enable`
3. Перезапусти бота

### Ошибка: "CHAT_ADMIN_REQUIRED"

**Причина:** Бот не является администратором или не имеет прав "Manage Topics"

**Решение:**
1. Убедись, что бот добавлен как администратор
2. Проверь права бота: должно быть включено **"Manage Topics"**
3. Перезапусти бота

### Бот не создает топики

1. Проверь, что `SUPPORT_CHAT_ID` установлен правильно
2. Убедись, что чат является форумом (Topics включены)
3. Проверь права бота: должно быть "Manage Topics"
4. Проверь логи бота на наличие ошибок

### Менеджер не может ответить пользователю

1. Убедись, что менеджер пишет в правильном топике
2. Проверь, что сообщение содержит текст или медиа
3. Убедись, что пользователь не заблокировал бота
4. Проверь логи бота на ошибки

### Сообщения теряются после перезапуска

Это нормально - связи хранятся в БД и восстанавливаются автоматически. Если проблема сохраняется:

1. Проверь подключение к БД
2. Убедись, что таблица `support_topics` создана
3. Проверь логи на ошибки БД

## 📝 Примеры использования

### Команда /support

Пользователь отправляет `/support` → бот объясняет, что можно писать → пользователь пишет вопрос → создается/используется топик → сообщение отправляется в топик.

### Прямое сообщение

Пользователь просто пишет боту → автоматически создается/используется топик для пользователя → сообщение отправляется в топик (если это не команда).

### Ответ менеджера

Менеджер пишет ответ в топике пользователя → бот автоматически определяет пользователя по `message_thread_id` → отправляет ответ пользователю → менеджер получает подтверждение.

## 🎯 Преимущества форум-топиков

- ✅ Каждый пользователь имеет свой отдельный топик
- ✅ Легко отслеживать диалоги с конкретными пользователями
- ✅ Менеджеры могут работать параллельно с разными пользователями
- ✅ История диалога сохраняется в топике
- ✅ Удобная навигация по топикам

## ⚠️ Важные замечания

1. **Чат ОБЯЗАТЕЛЬНО должен быть форумом** - без этого система не будет работать
2. **Бот должен иметь права "Manage Topics"** - без этого не получится создавать топики
3. **Каждый пользователь получает один топик** - все его сообщения идут в один топик
4. **Подтверждающее сообщение пользователю НЕ отправляется** (по требованию)

---

**Готово!** Система поддержки настроена и готова к работе. 🎉
