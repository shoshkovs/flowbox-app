# 🌸 FlowBox - Telegram MiniApp магазин цветов

Telegram MiniApp для заказа цветов с красивым интерфейсом и полным функционалом корзины и оформления заказов.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=8488111859:AAHEXqwUXwA8up6YyDxz14AcjemwzCa2dbI
WEBAPP_URL=http://localhost:3000
PORT=3000
```

**Важно:** Для продакшена замените `WEBAPP_URL` на публичный URL вашего сайта (например, `https://yourdomain.com`).

### 3. Запуск приложения

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

### 4. Настройка MiniApp в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Найдите вашего бота `@FlowboxBot`
3. Отправьте команду `/newapp` или выберите "Mini Apps" в меню
4. Укажите URL вашего приложения:
   - Для локальной разработки: `http://localhost:3000` (требует ngrok или аналог)
   - Для продакшена: ваш публичный URL

## 📁 Структура проекта

```
FlowBox/
├── bot.js              # Backend: Telegram бот и Express сервер
├── public/             # Frontend: MiniApp интерфейс
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили
│   └── app.js          # Логика приложения
├── package.json        # Зависимости проекта
└── README.md          # Документация
```

## ✨ Функционал

- ✅ Каталог товаров с изображениями
- ✅ Корзина покупок
- ✅ Оформление заказа с формой
- ✅ Интеграция с Telegram WebApp API
- ✅ Адаптивный дизайн
- ✅ Тактильная обратная связь (вибрация)

## 🔧 API Endpoints

### GET `/api/products`
Возвращает список всех товаров.

### POST `/api/orders`
Создает новый заказ.

**Тело запроса:**
```json
{
  "items": [
    {
      "id": 1,
      "name": "Букет роз",
      "price": 2500,
      "quantity": 2
    }
  ],
  "total": 5000,
  "name": "Иван Иванов",
  "phone": "+7 (999) 123-45-67",
  "address": "Москва, ул. Примерная, д. 1, кв. 1",
  "comment": "Доставить до 18:00"
}
```

## 🌐 Деплой для продакшена

### Вариант 1: Vercel / Netlify

1. Загрузите проект на GitHub
2. Подключите репозиторий к Vercel/Netlify
3. Укажите `WEBAPP_URL` в `.env` как URL вашего деплоя
4. Обновите настройки MiniApp в @BotFather

### Вариант 2: VPS / Cloud

1. Установите Node.js на сервер
2. Клонируйте проект
3. Установите зависимости: `npm install`
4. Настройте `.env` с публичным URL
5. Запустите через PM2: `pm2 start bot.js`

### Вариант 3: Локальная разработка с ngrok

Для тестирования локально:

```bash
# Установите ngrok
npm install -g ngrok

# Запустите туннель
ngrok http 3000

# Используйте полученный URL (например, https://abc123.ngrok.io) 
# в WEBAPP_URL и в настройках MiniApp
```

## 📱 Использование

1. Откройте бота `@FlowboxBot` в Telegram
2. Отправьте команду `/start`
3. Нажмите кнопку "🛍️ Открыть магазин"
4. Выберите товары и оформите заказ!

## 🎨 Кастомизация

### Изменение товаров

Отредактируйте массив `products` в файле `bot.js` (строка ~20).

### Изменение цветов

Измените CSS переменные в `public/styles.css`:

```css
:root {
    --primary-color: #ff6b9d;    /* Основной цвет */
    --secondary-color: #c44569;  /* Вторичный цвет */
}
```

## 🔐 Безопасность

- ⚠️ **Не публикуйте** файл `.env` в репозиторий
- ⚠️ **Не делитесь** токеном бота публично
- ✅ Используйте HTTPS для продакшена
- ✅ Валидируйте данные на сервере

## 📝 TODO

- [ ] Добавить базу данных для хранения заказов
- [ ] Добавить админ-панель для управления товарами
- [ ] Интегрировать платежи через Telegram Payments
- [ ] Добавить уведомления администратору о новых заказах
- [ ] Добавить систему отзывов

## 📄 Лицензия

ISC

## 👤 Автор

FlowBox Team

---

**Приятных покупок! 🌸**

