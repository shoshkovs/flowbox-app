# 📚 Полная документация: Адреса и Поддержка

**Дата создания:** 2025-12-09  
**Версия:** 1.0

---

# ЧАСТЬ 1: УПРАВЛЕНИЕ АДРЕСАМИ

## 1.1 Страница "Мои адреса" с редактированием и удалением

### 📋 Описание
На шаге 4 (Итого) при нажатии на стрелку рядом с адресом доставки открывается отдельная страница "Мои адреса" со списком всех сохраненных адресов. У каждого адреса есть меню (три точки) с кнопками "Изменить" и "Удалить".

### 🎯 UX Flow

#### Шаг 1: Открытие списка адресов
- Пользователь на шаге 4 нажимает на **стрелку** рядом с блоком "Адрес доставки"
- Открывается отдельная страница **"Мои адреса"**
- Дизайн страницы аналогичен вкладке 2 (выбор адреса)

#### Шаг 2: Просмотр и выбор адреса
- Отображается список всех сохраненных адресов
- Каждый адрес представлен в виде карточки с полным адресом
- Справа у каждого адреса есть иконка **⋮ (три точки)**
- При клике на саму карточку адреса - выбирается этот адрес и происходит возврат на шаг 4

#### Шаг 3: Меню адреса (три точки)
При нажатии на иконку ⋮ открывается выпадающее меню с кнопками:
- **Изменить** - открывает форму редактирования с предзаполненными данными
- **Удалить** - удаляет адрес с подтверждением

#### Шаг 4: Редактирование адреса
- Открывается форма редактирования с предзаполненными данными выбранного адреса
- Все поля заполнены: город, улица/дом, квартира, этаж, подъезд, домофон, комментарий
- После нажатия "Сохранить":
  - Адрес **обновляется** в БД (выполняется UPDATE, не создается новый)
  - Происходит возврат на шаг 4 (Итого)
  - В блоке "Адрес доставки" отображается обновленный адрес

#### Шаг 5: Удаление адреса
- Появляется подтверждение: "Вы уверены, что хотите удалить этот адрес?"
- После подтверждения адрес удаляется из БД
- Список адресов обновляется автоматически
- Если был удален выбранный адрес, он убирается из `checkoutData.address`

#### Шаг 6: Добавление нового адреса
- Внизу списка кнопка **"+ Добавить новый адрес"**
- Открывается форма создания нового адреса (переход на вкладку адресов)
- После сохранения новый адрес появляется в списке

### 🛠 Технические детали

#### HTML структура (`public/index.html`)

**Вкладка "Мои адреса":**
```html
<div class="tab-content" id="myAddressesTab" style="display: none;">
    <div class="address-page">
        <div class="address-page-header">
            <h2>Мои адреса</h2>
        </div>
        <div class="address-page-body">
            <div class="addresses-list" id="myAddressesList">
                <!-- Адреса будут добавлены динамически -->
            </div>
        </div>
        <div class="address-page-footer">
            <button type="button" class="submit-address-btn" id="addNewAddressFromListBtn">
                + Добавить новый адрес
            </button>
        </div>
    </div>
</div>
```

**Структура элемента адреса в списке:**
- Карточка адреса с полным текстом адреса
- Иконка ⋮ справа
- Выпадающее меню с кнопками "Изменить" и "Удалить"

#### JavaScript функции (`public/app.js`)

**Основные функции:**

1. **`openMyAddressesPage()`**
   - Открывает страницу "Мои адреса"
   - Вызывает `renderMyAddressesList()` для рендеринга списка
   - Настраивает BackButton для возврата на шаг 4
   - Скрывает все другие вкладки и шаги checkout

2. **`renderMyAddressesList()`**
   - Рендерит список всех сохраненных адресов
   - Фильтрует адреса без ID (показывает только валидные)
   - Для каждого адреса создает карточку с меню
   - Определяет, какой адрес выбран (по ID или содержимому)
   - Отображает пометку "Выбран" для текущего адреса

3. **`selectAddressFromMyAddresses(addressId)`**
   - Выбирает адрес из списка
   - Обновляет `checkoutData.address` с сохранением ID
   - Закрывает вкладку "Мои адреса"
   - Скрывает все вкладки
   - Возвращает на шаг 4 с обновленным адресом

4. **`editAddressFromMyAddresses(addressId)`**
   - Открывает форму редактирования для выбранного адреса
   - Закрывает меню и вкладку "Мои адреса"
   - Вызывает `openEditAddressPageFromList(address)` с данными адреса

5. **`deleteAddressFromMyAddresses(addressId)`**
   - Удаляет адрес с подтверждением
   - Обновляет `savedAddresses` (удаляет из массива)
   - Сохраняет изменения через `saveUserData()`
   - Перезагружает список адресов через `loadSavedAddresses()`
   - Обновляет отображение списка через `renderMyAddressesList()`

6. **`toggleAddressMenu(addressId)`**
   - Переключает выпадающее меню для адреса
   - Закрывает все другие открытые меню

7. **`openEditAddressPageFromList(address)`**
   - Открывает форму редактирования с предзаполненными данными
   - Сохраняет `address.id` в `editAddressTab.dataset.editingAddressId`
   - Парсит адрес из разных форматов (обратная совместимость)
   - Настраивает BackButton для возврата к списку адресов

8. **`saveEditAddress()`**
   - Сохраняет отредактированный адрес
   - Проверяет наличие `editingAddressId` в `editAddressTab.dataset`
   - Если ID есть - обновляет существующий адрес в `savedAddresses`
   - Если ID нет - создается новый адрес
   - Вызывает `saveUserData()` для сохранения на сервер
   - Вызывает `loadSavedAddresses()` для обновления списка
   - Обновляет `checkoutData.address` с сохранением ID
   - Возвращает на шаг 4

#### База данных

**Таблица `addresses`:**
```sql
CREATE TABLE addresses (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL,
  name TEXT,
  city TEXT NOT NULL,
  street TEXT NOT NULL,
  house TEXT,
  entrance TEXT,
  apartment TEXT,
  floor TEXT,
  intercom TEXT,
  comment TEXT,
  is_default BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

**Операции:**
- **UPDATE** - при редактировании существующего адреса (по `id`)
- **INSERT** - при добавлении нового адреса
- **DELETE** - при удалении адреса (по `id`)

#### API эндпоинты (`bot.js`)

**`POST /api/user-data`**
- Принимает массив `addresses` в теле запроса
- Обрабатывает адреса с ID через `UPDATE`
- Обрабатывает адреса без ID через `INSERT`
- Удаляет адреса, которых нет в новом списке

**Логика обработки адресов (`saveUserAddresses`):**
1. Загружаются существующие адреса из БД
2. Адреса с ID → `addressesToUpdate` (UPDATE)
3. Адреса без ID → проверка дубликатов → `addressesToInsert` (INSERT)
4. Удаляются адреса, которых нет в новом списке
5. Выполняются UPDATE и INSERT в одной транзакции

#### Предотвращение дубликатов

**Фильтрация адресов без ID:**
- При загрузке адресов (`loadSavedAddresses`) - фильтруются адреса без ID
- При сохранении (`saveUserData`) - фильтруются адреса без ID перед отправкой
- При отображении в списке - показываются только адреса с валидным ID

**Нормализация адресов:**
- Функция `parseStreetAndHouse()` извлекает номер дома из поля "улица"
- Функция `isAddressDuplicate()` сравнивает адреса по city, street, house, apartment

---

## 1.2 Placeholder для полей "Улица, дом"

### 📋 Описание
Обновлен placeholder для всех полей ввода адреса (улица, дом) для единообразия.

### Изменения

**Старый placeholder:**
```
"Введите улицу, дом (например: Невский проспект 10к2)"
```

**Новый placeholder:**
```
"Например Невский проспект, 10к2"
```

### Файлы с обновленным placeholder

1. **`public/index.html`** - поле `addressStreet` (вкладка адресов)
2. **`public/index.html`** - поле `orderAddressStreet` (шаг 2 оформления)
3. **`public/index.html`** - поле `editAddressStreet` (редактирование адреса)

---

## 1.3 Навигация и возврат

### Логика навигации

**После сохранения адреса:**
1. Скрывается форма редактирования (`editAddressTab.style.display = 'none'`)
2. Скрываются все вкладки (`document.querySelectorAll('.tab-content').forEach(...)`)
3. Вызывается `renderCheckoutSummary()` для обновления отображения
4. Вызывается `goToStep(4)` для возврата на шаг 4

**После выбора адреса:**
1. Обновляется `checkoutData.address` с сохранением ID
2. Закрывается вкладка "Мои адреса"
3. Скрываются все вкладки
4. Вызывается `renderCheckoutSummary()` и `goToStep(4)`

**BackButton:**
- На странице "Мои адреса" - возврат на шаг 4
- На форме редактирования - возврат к списку адресов

---

# ЧАСТЬ 2: КОММЕНТАРИЙ К ЗАКАЗУ

## 2.1 Поле "Комментарий: Особые пожелания к заказу"

### 📋 Описание
Поле для комментария клиента, отображаемое на шаге 4 (Итого) после корзины, перед итоговой суммой.

### 🎯 UX

**Расположение:**
- **Шаг 4 (Итого)**: После блока "Корзина", перед "Итого к оплате"
- Заголовок: **"Комментарий"**
- Подзаголовок/плейсхолдер: **"Особые пожелания к заказу"**
- Тип: `textarea`, 2-3 строки

**Отображение:**
```html
<div class="checkout-summary-item">
    <div class="summary-item-content" style="flex-direction: column; align-items: flex-start; gap: 8px;">
        <div class="summary-item-label">Комментарий</div>
        <textarea id="orderCommentField" 
                  placeholder="Особые пожелания к заказу" 
                  rows="3"
                  style="width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; font-size: 16px; font-family: inherit; resize: vertical; min-height: 80px;"></textarea>
    </div>
</div>
```

**Функционал:**
- Пользователь может ввести комментарий на шаге 4
- Комментарий сохраняется в `checkoutData.orderComment`
- Синхронизируется автоматически при вводе
- Отправляется на сервер при оформлении заказа

### 🛠 Технические детали

#### Хранение данных

**`checkoutData` объект:**
```javascript
let checkoutData = {
    recipientName: '',
    recipientPhone: '',
    address: {},
    deliveryDate: '',
    deliveryTime: '',
    orderComment: '', // Комментарий. Особые пожелания к заказу
    leaveAtDoor: false
};
```

#### Синхронизация (`public/app.js`)

**Инициализация в `initCheckoutSteps()`:**
```javascript
const orderCommentField = document.getElementById('orderCommentField');
if (orderCommentField) {
    // Заполняем поле из checkoutData при загрузке
    if (checkoutData.orderComment) {
        orderCommentField.value = checkoutData.orderComment;
    }
    
    // Обновляем checkoutData при изменении
    orderCommentField.addEventListener('input', () => {
        checkoutData.orderComment = orderCommentField.value.trim();
    });
}
```

**Отображение в `renderCheckoutSummary()`:**
```javascript
const orderCommentField = document.getElementById('orderCommentField');
if (orderCommentField) {
    orderCommentField.value = checkoutData.orderComment || '';
}
```

**Отправка в `validateAndSubmitOrder()`:**
```javascript
// Используем комментарий из checkoutData (синхронизирован с полем на шаге 4)
const comment = checkoutData.orderComment || (commentField ? commentField.value.trim() : '');
```

#### База данных

**Таблица `orders`:**
```sql
user_comment TEXT  -- Комментарий. Особые пожелания к заказу
courier_comment TEXT  -- Комментарий для курьера (отдельное поле)
```

**Важно:** `user_comment` (комментарий клиента) и `courier_comment` (комментарий для курьера) - это **разные поля**.

#### API (`bot.js`)

**`POST /api/orders`:**
- Принимает поле `comment` или `userComment`
- Сохраняется в `orders.user_comment`:
```javascript
const userComment = orderData.userComment || orderData.comment || null;

await client.query(
  `INSERT INTO orders (..., user_comment, ...) VALUES (..., $19, ...)`,
  [..., userComment, ...]
);
```

#### Админка

**Файл:** `admin/src/components/orders/OrderDetail.jsx`

**Отображение:**
- В карточке заказа есть блок "Комментарии"
- Комментарий клиента отображается в поле "Особые пожелания к заказу"
- Редактируемое поле для менеджера

---

# ЧАСТЬ 3: ЧЕКБОКС "ОСТАВИТЬ У ДВЕРИ"

## 3.1 Описание
Новая опция для заказов: пользователь может указать, что заказ нужно оставить у двери без встречи с получателем.

### 🎯 UX

**Расположение:**

1. **Шаг 3 (Дата и время)**:
   - После выбора времени доставки
   - Перед кнопкой "Продолжить"
   - Интерактивный чекбокс для изменения

2. **Шаг 4 (Итого)**:
   - В блоке "Доставка"
   - После "Дата и время доставки"
   - Только отображение (не редактируется)

**HTML структура:**

**Шаг 3:**
```html
<div class="form-group" style="margin-top: 16px;">
    <label style="display: flex; align-items: center; gap: 8px; cursor: pointer;">
        <input type="checkbox" id="leaveAtDoorCheckbox" style="width: 20px; height: 20px; cursor: pointer;">
        <span>Оставить у двери</span>
    </label>
</div>
```

**Шаг 4:**
```html
<div class="checkout-summary-item">
    <div class="summary-item-content">
        <div class="summary-item-label">Оставить у двери</div>
        <div class="summary-item-value" id="summaryLeaveAtDoor">Нет</div>
    </div>
</div>
```

### 🛠 Технические детали

#### Хранение данных

**`checkoutData` объект:**
```javascript
checkoutData.leaveAtDoor = false; // По умолчанию false
```

#### Синхронизация (`public/app.js`)

**Инициализация в `initCheckoutSteps()`:**
```javascript
const leaveAtDoorCheckbox = document.getElementById('leaveAtDoorCheckbox');
if (leaveAtDoorCheckbox) {
    // Заполняем чекбокс из checkoutData при загрузке
    leaveAtDoorCheckbox.checked = checkoutData.leaveAtDoor || false;
    
    // Обновляем checkoutData при изменении
    leaveAtDoorCheckbox.addEventListener('change', () => {
        checkoutData.leaveAtDoor = leaveAtDoorCheckbox.checked;
        renderCheckoutSummary(); // Обновляем отображение на шаге 4
    });
}
```

**Обновление при переходе на шаг 3:**
```javascript
if (step === 3) {
    const leaveAtDoorCheckbox = document.getElementById('leaveAtDoorCheckbox');
    if (leaveAtDoorCheckbox) {
        leaveAtDoorCheckbox.checked = checkoutData.leaveAtDoor || false;
    }
}
```

**Отображение на шаге 4:**
```javascript
// В renderCheckoutSummary()
const summaryLeaveAtDoorEl = document.getElementById('summaryLeaveAtDoor');
if (summaryLeaveAtDoorEl) {
    summaryLeaveAtDoorEl.textContent = checkoutData.leaveAtDoor ? 'Да' : 'Нет';
}
```

**Отправка в `validateAndSubmitOrder()`:**
```javascript
// Используем значение из checkoutData (синхронизирован с чекбоксом на шаге 3)
const leaveAtDoor = checkoutData.leaveAtDoor || false;

const orderData = {
    // ...
    leaveAtDoor: leaveAtDoor, // Оставить у двери
    // ...
};
```

#### База данных

**Миграция:**
```sql
ALTER TABLE orders
ADD COLUMN leave_at_door BOOLEAN NOT NULL DEFAULT FALSE;
```

**Автоматическая миграция в `bot.js`:**
- Проверяется наличие колонки при запуске
- Если колонки нет - создается автоматически
- Выполняется через 9 секунд после подключения к БД

#### API (`bot.js`)

**`POST /api/orders`:**
```javascript
const leaveAtDoor = orderData.leaveAtDoor || false;

await client.query(
  `INSERT INTO orders (..., leave_at_door, ...) VALUES (..., $21, ...)`,
  [..., leaveAtDoor, ...]
);
```

**`GET /api/admin/orders/:id`:**
```javascript
const response = {
    // ...
    leave_at_door: order.leave_at_door || false,
    // ...
};
```

#### Админка

**Файл:** `admin/src/components/orders/OrderDetail.jsx`

**Отображение:**
- В блоке "Доставка"
- После адреса доставки
- Иконка 🚪 и текст "Оставить у двери" (если `leave_at_door = true`)

**Код:**
```jsx
{order.leave_at_door && (
  <div className="flex items-start gap-3">
    <div className="w-5 h-5 mt-0.5 flex-shrink-0 flex items-center justify-center">
      <span className="text-lg">🚪</span>
    </div>
    <div>
      <p className="text-sm font-medium text-pink-600">Оставить у двери</p>
    </div>
  </div>
)}
```

---

# ЧАСТЬ 4: СИСТЕМА ПОДДЕРЖКИ (ФОРУМ-ТОПИКИ)

## 4.1 Описание
Система поддержки на основе форум-топиков Telegram. Каждому пользователю создается отдельный топик в форум-чате поддержки. Все сообщения пользователя идут в его топик, менеджеры отвечают в топике, ответы автоматически отправляются пользователю.

### 🎯 UX Flow

#### Для пользователя:

1. Пользователь пишет боту лично (команда `/support` или просто сообщение)
2. Бот автоматически создает или находит форум-топик для этого пользователя
3. Сообщение отправляется в топик с информацией о пользователе
4. **Подтверждающее сообщение НЕ отправляется** (по требованию)

#### Для менеджера:

1. В форум-чате поддержки для каждого пользователя создается отдельный топик:
   - Название топика: `Тикет {user_id} ({username})`
   - Пример: `Тикет 1059138125 (@shoshkovs)`

2. В топике отображается шапка с информацией о пользователе:
   ```
   👤 Новый запрос в поддержку
   
   👤 Имя: Саша
   🆔 ID: 1059138125
   📝 Username: @shoshkovs
   📱 Телефон: +7 (965) 080-08-13
   📧 Email: alexansho@gmail.com
   ```

3. Ниже отправляется само сообщение пользователя

4. Менеджер пишет ответ прямо в топике (не обязательно делать Reply)

5. Бот автоматически определяет пользователя по `message_thread_id` и отправляет ответ

6. Менеджер получает подтверждение: "✅ Ответ отправлен пользователю"

### 🚀 Настройка

#### Шаг 1: Создание форум-чата поддержки

⚠️ **ВАЖНО:** Чат поддержки должен быть **форумом** (супергруппой с включенными Topics)!

1. Создай **супергруппу** в Telegram (например, "FlowBox Support")
2. **Включи режим "Topics" (Форум):**
   - Открой настройки группы
   - `Профиль чата → Edit → Topics/Форум → Enable`
3. Добавь туда бота (`@FlowboxBot` или твой бот)
4. Дай боту права **администратора** с правами:
   - ✅ **Manage Topics** (Управление темами) - **ОБЯЗАТЕЛЬНО!**
   - ✅ **Send messages** (Отправка сообщений)
   - ✅ **Delete messages** (желательно)
5. Добавь менеджеров в эту группу

#### Шаг 2: Получение ID чата поддержки

**Способ 1: Через бота**
1. Отправь любое сообщение в группу поддержки
2. В логах бота найди строку с `ctx.chat.id` - это будет отрицательное число (например, `-1001234567890`)
3. Это и есть `SUPPORT_CHAT_ID`

**Способ 2: Через @userinfobot**
1. Добавь `@userinfobot` в группу поддержки
2. Отправь команду `/start`
3. Бот покажет ID группы (будет отрицательное число)

**Способ 3: Через API**
1. Отправь сообщение в группу
2. Открой `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
3. Найди `"chat":{"id":-1001234567890}` - это и есть ID чата

#### Шаг 3: Настройка переменной окружения

Добавь в `.env` или в настройки Render.com:

```env
SUPPORT_CHAT_ID=-1001234567890
```

⚠️ **Важно:** ID должен быть числом (может быть отрицательным для групп).

#### Шаг 4: Перезапуск бота

После добавления переменной окружения перезапусти бота. В логах должно появиться:

```
✅ Чат поддержки настроен: -1001234567890
💡 Убедитесь, что:
   1. Чат является форумом (Topics включены)
   2. Бот имеет права администратора
   3. У бота есть права "Manage Topics" и "Send messages"
```

Если переменная не установлена, будет предупреждение:

```
⚠️  SUPPORT_CHAT_ID не установлен. Система поддержки будет недоступна.
```

### 🛠 Технические детали

#### База данных

**Таблица `support_topics`:**
```sql
CREATE TABLE support_topics (
  id SERIAL PRIMARY KEY,
  user_id BIGINT NOT NULL,              -- ID пользователя Telegram
  message_thread_id INTEGER NOT NULL,   -- ID топика (thread_id из Telegram)
  topic_name TEXT,                       -- Название топика
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id),
  UNIQUE(message_thread_id)
);
```

**Индексы:**
```sql
CREATE INDEX idx_support_topics_user_id ON support_topics(user_id);
CREATE INDEX idx_support_topics_message_thread_id ON support_topics(message_thread_id);
```

**Автоматическая миграция:**
- Таблица создается автоматически при первом запуске
- Если колонки `message_thread_id` или `topic_name` отсутствуют, они создаются автоматически
- Миграция выполняется через 8 секунд после подключения к БД

#### API методы Telegram

**`createForumTopic`:**
- Создает новый форум-топик в чате
- Возвращает объект с `message_thread_id`
- Используется для создания топика для нового пользователя

**`sendMessage` с `message_thread_id`:**
- Отправляет сообщение в конкретный топик
- Обязательный параметр: `message_thread_id`

**`copyMessage` с `message_thread_id`:**
- Копирует медиа-сообщение в конкретный топик
- Используется для фото, видео, документов, голосовых

#### Функции (`bot.js`)

**1. `getOrCreateSupportTopic(userId, userName, username)`:**
```javascript
async function getOrCreateSupportTopic(userId, userName, username) {
  // 1. Проверяем, есть ли уже топик для этого пользователя
  const existingTopic = await client.query(
    'SELECT message_thread_id FROM support_topics WHERE user_id = $1::bigint',
    [userId]
  );
  
  if (existingTopic.rows.length > 0) {
    // Возвращаем существующий топик
    return existingTopic.rows[0].message_thread_id;
  }
  
  // 2. Создаем новый топик
  const topicName = `Тикет ${userId} (${username || userName || 'Пользователь'})`;
  const topic = await bot.telegram.callApi('createForumTopic', {
    chat_id: SUPPORT_CHAT_ID,
    name: topicName
  });
  
  const messageThreadId = topic.message_thread_id;
  
  // 3. Сохраняем топик в БД
  await client.query(
    `INSERT INTO support_topics (user_id, message_thread_id, topic_name, updated_at)
     VALUES ($1::bigint, $2::integer, $3::text, now())
     ON CONFLICT (user_id) DO UPDATE SET
       message_thread_id = EXCLUDED.message_thread_id,
       topic_name = EXCLUDED.topic_name,
       updated_at = now()
     ON CONFLICT (message_thread_id) DO UPDATE SET
       user_id = EXCLUDED.user_id,
       topic_name = EXCLUDED.topic_name,
       updated_at = now()`,
    [userId, messageThreadId, topicName]
  );
  
  // 4. Дополнительное обновление связи (на случай, если топик уже существовал)
  await client.query(
    `UPDATE support_topics 
     SET user_id = $1::bigint, topic_name = $3::text, updated_at = now()
     WHERE message_thread_id = $2::integer AND (user_id IS NULL OR user_id != $1::bigint)`,
    [userId, messageThreadId, topicName]
  );
  
  return messageThreadId;
}
```

**2. `getUserIdByThreadId(messageThreadId)`:**
```javascript
async function getUserIdByThreadId(messageThreadId) {
  const result = await client.query(
    'SELECT user_id FROM support_topics WHERE message_thread_id = $1::integer',
    [messageThreadId]
  );
  return result.rows.length > 0 ? result.rows[0].user_id : null;
}
```

**3. Обработчик личных сообщений:**
```javascript
bot.on('message', async (ctx) => {
  // 1) Личный чат с пользователем
  if (chat.type === 'private') {
    // Пропускаем команды
    if (ctx.message.text && ctx.message.text.startsWith('/')) {
      return;
    }
    
    // Получаем или создаем топик
    const messageThreadId = await getOrCreateSupportTopic(userId, userName, username);
    
    if (!messageThreadId) {
      await ctx.reply('⚠️ Произошла ошибка при создании обращения в поддержку. Попробуйте позже.');
      return;
    }
    
    // Отправляем шапку (только для первого сообщения)
    if (shouldSendHeader) {
      await bot.telegram.sendMessage(
        SUPPORT_CHAT_ID,
        header,
        {
          parse_mode: 'HTML',
          message_thread_id: messageThreadId
        }
      );
    }
    
    // Отправляем сообщение пользователя в топик
    await bot.telegram.sendMessage(
      SUPPORT_CHAT_ID,
      `📨 <b>Сообщение:</b>\n${ctx.message.text}`,
      {
        parse_mode: 'HTML',
        message_thread_id: messageThreadId
      }
    );
    
    // НЕ отправляем подтверждающее сообщение (по требованию)
  }
});
```

**4. Обработчик ответов менеджеров:**
```javascript
// 2) Сообщение в чате поддержки (форум)
if (SUPPORT_CHAT_ID && chat.id === SUPPORT_CHAT_ID) {
  const messageThreadId = ctx.message.message_thread_id;
  
  if (!messageThreadId) {
    // Сообщение не в топике, игнорируем
    return;
  }
  
  // Получаем userId по message_thread_id из БД
  let userId = await getUserIdByThreadId(messageThreadId);
  
  if (!userId) {
    // Fallback: ищем userId в тексте шапки топика
    const replyText = ctx.message.reply_to_message?.text || ctx.message.text || '';
    const userIdMatch = replyText.match(/🆔.*?<code>(\d+)<\/code>|🆔.*?ID.*?(\d+)|Тикет\s+(\d+)/i);
    
    if (userIdMatch) {
      const foundUserId = parseInt(userIdMatch[1] || userIdMatch[2] || userIdMatch[3]);
      // Сохраняем связь для будущего использования
      await client.query(
        `INSERT INTO support_topics (user_id, message_thread_id, updated_at)
         VALUES ($1::bigint, $2::integer, now())
         ON CONFLICT (message_thread_id) DO UPDATE SET
           user_id = EXCLUDED.user_id,
           updated_at = now()`,
        [foundUserId, messageThreadId]
      );
      userId = foundUserId;
    }
  }
  
  if (userId) {
    // Отправляем ответ пользователю
    await sendManagerReplyToUser(ctx, userId);
  } else {
    await ctx.reply('⚠️ Не удалось определить пользователя для этого топика.');
  }
}
```

**5. `sendManagerReplyToUser(ctx, userId)`:**
```javascript
async function sendManagerReplyToUser(ctx, userId) {
  const messageText = ctx.message.text || ctx.message.caption || '';
  
  // Пропускаем команды
  if (messageText.startsWith('/')) {
    return;
  }
  
  // Если есть медиа, копируем его
  if (ctx.message.photo || ctx.message.document || ctx.message.video || ctx.message.voice) {
    await bot.telegram.copyMessage(
      userId,
      ctx.chat.id,
      ctx.message.message_id
    );
  } else {
    // Отправляем текстовый ответ
    await bot.telegram.sendMessage(
      userId,
      `💬 <b>Ответ от поддержки:</b>\n\n${messageText}`,
      { parse_mode: 'HTML' }
    );
  }
  
  // Подтверждаем менеджеру
  await ctx.reply('✅ Ответ отправлен пользователю', { 
    reply_to_message_id: ctx.message.message_id 
  });
}
```

### 🐛 Устранение проблем

#### Ошибка: "CHAT_FORUM_REQUIRED"

**Причина:** Чат не является форумом (Topics не включены)

**Решение:**
1. Открой настройки группы
2. `Профиль чата → Edit → Topics/Форум → Enable`
3. Перезапусти бота

#### Ошибка: "CHAT_ADMIN_REQUIRED"

**Причина:** Бот не является администратором или не имеет прав "Manage Topics"

**Решение:**
1. Убедись, что бот добавлен как администратор
2. Проверь права бота: должно быть включено **"Manage Topics"**
3. Перезапусти бота

#### Ошибка: "column 'message_thread_id' does not exist"

**Причина:** Таблица создана, но колонка отсутствует

**Решение:** 
- Миграция автоматически создаст колонку при следующем запуске
- Проверь логи на наличие сообщения: "🔄 Добавляем колонку message_thread_id..."

#### Ошибка: "column 'topic_name' does not exist"

**Причина:** Таблица создана, но колонка отсутствует

**Решение:**
- Миграция автоматически создаст колонку при следующем запуске
- Проверь логи на наличие сообщения: "🔄 Добавляем колонку topic_name..."

#### Ошибка: "Не удалось определить пользователя для этого топика"

**Причина:** Топик существует, но связь с пользователем не найдена в БД

**Fallback механизм:**
1. Система пытается извлечь userId из текста шапки топика
2. Ищет паттерны: `🆔 ID: 123456789`, `Тикет 123456789`
3. Если найден - сохраняет связь в БД для будущего использования
4. Отправляет ответ пользователю

**Если не помогло:**
1. Проверь, что шапка топика содержит ID пользователя
2. Проверь логи на ошибки БД
3. Убедись, что таблица `support_topics` создана и содержит данные

### 📝 Поддерживаемые типы сообщений

- ✅ Текст
- ✅ Фото
- ✅ Документы
- ✅ Видео
- ✅ Голосовые сообщения

Для медиа используется `copyMessage` с `message_thread_id`. Если копирование не удается, отправляется текстовое описание.

---

# ЧАСТЬ 5: ОБЩИЕ ТЕХНИЧЕСКИЕ ДЕТАЛИ

## 5.1 Навигация

### Функция `goToStep(step)`

**Логика:**
1. Скрывает все шаги checkout
2. Скрывает все вкладки (включая редактирование адреса, получателя, список адресов)
3. Показывает нужный шаг
4. Обновляет индикатор прогресса
5. Устанавливает BackButton в зависимости от шага

**Особенности:**
- При переходе на шаг 3 - синхронизирует чекбокс "Оставить у двери"
- При переходе на шаг 4 - вызывает `renderCheckoutSummary()`
- При переходе на шаг 2 - вызывает `renderCheckoutAddresses()`

## 5.2 Синхронизация данных

### Объект `checkoutData`

```javascript
let checkoutData = {
    recipientName: '',        // Имя получателя
    recipientPhone: '',       // Телефон получателя
    address: {                // Адрес доставки (объект)
        id: null,             // ID адреса (если выбран из сохраненных)
        city: '',
        street: '',
        house: '',
        apartment: '',
        floor: '',
        entrance: '',
        intercom: '',
        comment: ''
    },
    deliveryDate: '',         // Дата доставки
    deliveryTime: '',         // Время доставки
    orderComment: '',         // Комментарий. Особые пожелания к заказу
    leaveAtDoor: false        // Оставить у двери
};
```

**Синхронизация:**
- Данные сохраняются при переходе между шагами
- При перезагрузке данные восстанавливаются из localStorage
- Комментарий и чекбокс синхронизируются автоматически при изменении

## 5.3 Фильтрация адресов

### Предотвращение дубликатов

**Места фильтрации:**

1. **При загрузке адресов:**
```javascript
function loadSavedAddresses() {
    // Сначала фильтруем адреса без ID
    savedAddresses = savedAddresses.filter(addr => 
        addr.id && typeof addr.id === 'number' && addr.id > 0
    );
    // ...
}
```

2. **При сохранении на сервер:**
```javascript
async function saveUserData() {
    // Фильтруем адреса - убираем адреса без ID перед отправкой
    const addressesToSave = savedAddresses.filter(addr => {
        if (addr.id && typeof addr.id === 'number' && addr.id > 0) {
            return true;
        }
        console.warn('[saveUserData] ⚠️ Пропущен адрес без ID:', addr);
        return false;
    });
    // ...
}
```

3. **При загрузке с сервера:**
```javascript
if (data.addresses && Array.isArray(data.addresses)) {
    // Фильтруем только адреса с валидным ID
    savedAddresses = data.addresses.filter(addr => 
        addr.id && typeof addr.id === 'number' && addr.id > 0
    );
}
```

4. **При отображении в списке:**
```javascript
function renderMyAddressesList() {
    // Фильтруем адреса - показываем только адреса с валидным ID
    const validAddresses = savedAddresses.filter(addr => 
        addr.id && typeof addr.id === 'number' && addr.id > 0
    );
    // ...
}
```

## 5.4 Обработка ошибок

### Логирование

Все операции логируются с эмодзи для удобства:
- 🚀 Начало операции
- ✅ Успешное выполнение
- ⚠️ Предупреждение
- ❌ Ошибка
- 🔍 Поиск/проверка
- 📦 Работа с данными

### Try-Catch блоки

Все операции с БД обернуты в try-catch:
- Ошибки логируются с полным стеком
- Понятные сообщения об ошибках для пользователей
- Graceful degradation (приложение продолжает работу при ошибках)

---

# ЧАСТЬ 6: ПРОВЕРОЧНЫЕ ЧЕК-ЛИСТЫ

## 6.1 Чек-лист: Страница "Мои адреса"

- [ ] Открывается при клике на стрелку на шаге 4
- [ ] Отображаются все сохраненные адреса (только с валидным ID)
- [ ] Меню с тремя точками работает
- [ ] Кнопка "Изменить" открывает форму редактирования
- [ ] Кнопка "Удалить" удаляет адрес с подтверждением
- [ ] Редактирование обновляет существующий адрес (не создает новый)
- [ ] После сохранения возврат на шаг 4 (не белый экран)
- [ ] Выбор адреса возвращает на шаг 4
- [ ] Кнопка "Добавить новый адрес" работает
- [ ] BackButton возвращает на шаг 4

## 6.2 Чек-лист: Комментарий к заказу

- [ ] Поле отображается на шаге 4 после корзины
- [ ] Плейсхолдер: "Особые пожелания к заказу"
- [ ] Комментарий сохраняется в `checkoutData.orderComment`
- [ ] Комментарий отправляется на сервер при оформлении заказа
- [ ] Комментарий сохраняется в БД как `user_comment`
- [ ] Комментарий отображается в админке

## 6.3 Чек-лист: Чекбокс "Оставить у двери"

- [ ] Чекбокс отображается на шаге 3 после времени доставки
- [ ] Значение отображается на шаге 4 в блоке "Доставка"
- [ ] Значение синхронизируется между шагами 3 и 4
- [ ] Значение сохраняется в БД как `leave_at_door`
- [ ] Значение отправляется на сервер при оформлении заказа
- [ ] Отображается в админке (иконка 🚪 и текст)

## 6.4 Чек-лист: Система поддержки

- [ ] Чат является форумом (Topics включены)
- [ ] Бот имеет права "Manage Topics"
- [ ] `SUPPORT_CHAT_ID` установлен в переменных окружения
- [ ] Таблица `support_topics` создана в БД
- [ ] Топики создаются для новых пользователей
- [ ] Название топика: "Тикет {user_id} ({username})"
- [ ] Шапка с информацией о пользователе отправляется в топик
- [ ] Сообщения пользователей попадают в топик
- [ ] Ответы менеджеров отправляются пользователям
- [ ] Связь топик-пользователь сохраняется в БД
- [ ] Fallback механизм работает (поиск userId в тексте)
- [ ] Подтверждающее сообщение пользователю НЕ отправляется

---

# ЧАСТЬ 7: ФАЙЛЫ И КОД

## 7.1 Файлы фронтенда

### `public/index.html`

**Страница "Мои адреса":**
- Строки ~600-615: вкладка `myAddressesTab`

**Поле комментария:**
- Строки ~510-525: блок с `textarea` `id="orderCommentField"` на шаге 4

**Чекбокс "Оставить у двери":**
- Строки ~452-457: чекбокс на шаге 3
- Строки ~503-508: отображение на шаге 4

**Placeholder для адресов:**
- Строки ~249, ~373, ~566: поля "Улица, дом"

### `public/app.js`

**Функции управления адресами:**
- `openMyAddressesPage()` - строка ~5537
- `renderMyAddressesList()` - строка ~5569
- `selectAddressFromMyAddresses(addressId)` - строка ~5677
- `editAddressFromMyAddresses(addressId)` - строка ~5718
- `deleteAddressFromMyAddresses(addressId)` - строка ~5751
- `toggleAddressMenu(addressId)` - строка ~5660
- `openEditAddressPageFromList(address)` - строка ~5771
- `saveEditAddress()` - строка ~5845

**Функции комментария:**
- Синхронизация в `initCheckoutSteps()` - строка ~4846
- Отображение в `renderCheckoutSummary()` - строка ~5475
- Отправка в `validateAndSubmitOrder()` - строка ~2404

**Функции чекбокса:**
- Синхронизация в `initCheckoutSteps()` - строка ~4852
- Обновление в `goToStep()` - строка ~5035
- Отображение в `renderCheckoutSummary()` - строка ~5469

**Объекты данных:**
- `checkoutData` - строка ~4735
- `savedAddresses` - глобальный массив

## 7.2 Файлы бэкенда

### `bot.js`

**Миграции БД:**
- Строки ~704-750: миграция таблицы `support_topics`
- Строки ~752-783: миграция поля `leave_at_door`

**Функции поддержки:**
- `getOrCreateSupportTopic()` - строка ~6799
- `getUserIdByThreadId()` - строка ~6914
- `sendManagerReplyToUser()` - строка ~7315

**Обработчики:**
- `bot.on('message')` - строка ~7004
  - Личные сообщения: строки ~7009-7210
  - Сообщения в чате поддержки: строки ~7213-7307

**API эндпоинты:**
- `POST /api/user-data` - сохранение адресов
- `POST /api/orders` - создание заказа (с комментарием и `leave_at_door`)
- `GET /api/admin/orders/:id` - получение заказа (с `leave_at_door`)

**Функции работы с адресами:**
- `saveUserAddresses()` - строка ~1441
- `loadUserAddresses()` - строка ~1563
- `isAddressDuplicate()` - строка ~1346
- `parseStreetAndHouse()` - строка ~1325

## 7.3 Файлы админки

### `admin/src/components/orders/OrderDetail.jsx`

**Отображение `leave_at_door`:**
- Строки ~457-466: блок с иконкой 🚪 и текстом "Оставить у двери"

**Отображение комментария:**
- Строки ~578-590: поле "Особые пожелания к заказу"

---

# ЧАСТЬ 8: ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

## 8.1 Проблема: Белый экран после сохранения адреса

**Причина:** Не все вкладки скрываются перед возвратом на шаг 4

**Решение:**
```javascript
// В saveEditAddress() и selectAddressFromMyAddresses()
// Скрываем все вкладки перед возвратом
document.querySelectorAll('.tab-content').forEach(tab => {
    tab.style.display = 'none';
});

// В goToStep()
// Скрываем все вкладки при переходе на любой шаг
document.querySelectorAll('.tab-content').forEach(tab => {
    tab.style.display = 'none';
});
```

## 8.2 Проблема: Дубликаты адресов

**Причина:** Адреса без ID попадают в список и создаются как новые

**Решение:**
- Фильтрация адресов без ID во всех местах:
  - При загрузке с сервера
  - При сохранении на сервер
  - При отображении в списке
  - При загрузке из localStorage

## 8.3 Проблема: Редактирование создает новый адрес вместо обновления

**Причина:** ID адреса не сохраняется или теряется

**Решение:**
- Сохранение `address.id` в `editAddressTab.dataset.editingAddressId`
- Сохранение ID в `checkoutData.address.id` при выборе
- Проверка наличия ID при сохранении в `saveEditAddress()`

## 8.4 Проблема: Ошибка "column 'message_thread_id' does not exist"

**Причина:** Таблица создана до добавления миграции колонки

**Решение:**
- Автоматическая миграция проверяет наличие колонок
- Если колонок нет - создает их автоматически
- Адаптивные SQL-запросы проверяют доступность колонок перед использованием

## 8.5 Проблема: "Не удалось определить пользователя для этого топика"

**Причина:** Связь топик-пользователь не сохранена в БД

**Решение:**
- Fallback: поиск userId в тексте шапки топика
- Сохранение связи при обнаружении userId в тексте
- Дополнительное обновление связи при создании топика

---

# ЧАСТЬ 9: ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

## 9.1 Пример: Редактирование адреса

```javascript
// 1. Пользователь на шаге 4 нажимает стрелку рядом с адресом
openMyAddressesPage();

// 2. Открывается список адресов
renderMyAddressesList();

// 3. Пользователь нажимает три точки и "Изменить"
editAddressFromMyAddresses(720); // ID адреса

// 4. Открывается форма редактирования
openEditAddressPageFromList(address);

// 5. Пользователь меняет квартиру с "57" на "80" и нажимает "Сохранить"
saveEditAddress();

// 6. Адрес обновляется в savedAddresses и на сервере
savedAddresses[addressIndex] = { ...savedAddresses[addressIndex], apartment: '80' };
await saveUserData(); // UPDATE addresses SET apartment = '80' WHERE id = 720

// 7. Возврат на шаг 4 с обновленным адресом
goToStep(4);
```

## 9.2 Пример: Создание топика поддержки

```javascript
// 1. Пользователь пишет боту лично
bot.on('message', async (ctx) => {
  if (chat.type === 'private') {
    const userId = from.id; // 1059138125
    
    // 2. Получаем или создаем топик
    const messageThreadId = await getOrCreateSupportTopic(
      userId, 
      'Саша', 
      '@shoshkovs'
    );
    // Результат: messageThreadId = 14
    
    // 3. Создается топик в Telegram
    // Название: "Тикет 1059138125 (@shoshkovs)"
    
    // 4. Сохраняется в БД
    // INSERT INTO support_topics (user_id, message_thread_id, topic_name)
    // VALUES (1059138125, 14, 'Тикет 1059138125 (@shoshkovs)')
    
    // 5. Отправляется шапка в топик
    await bot.telegram.sendMessage(
      SUPPORT_CHAT_ID,
      '👤 Новый запрос в поддержку\n...',
      { message_thread_id: 14 }
    );
    
    // 6. Отправляется сообщение пользователя
    await bot.telegram.sendMessage(
      SUPPORT_CHAT_ID,
      '📨 Сообщение:\nтест1',
      { message_thread_id: 14 }
    );
  }
});
```

## 9.3 Пример: Ответ менеджера

```javascript
// 1. Менеджер пишет ответ в топике
bot.on('message', async (ctx) => {
  if (chat.id === SUPPORT_CHAT_ID) {
    const messageThreadId = ctx.message.message_thread_id; // 14
    
    // 2. Получаем userId по топику
    const userId = await getUserIdByThreadId(14);
    // Результат: userId = 1059138125
    
    // 3. Отправляем ответ пользователю
    await bot.telegram.sendMessage(
      1059138125,
      '💬 Ответ от поддержки:\n\nСпасибо за обращение!',
      { parse_mode: 'HTML' }
    );
    
    // 4. Подтверждаем менеджеру
    await ctx.reply('✅ Ответ отправлен пользователю');
  }
});
```

---

# ЧАСТЬ 10: ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

## Требуемые переменные

```env
# Базовые
BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://user:password@host:port/database
PORT=3000

# Поддержка
SUPPORT_CHAT_ID=-1001234567890  # ID форум-чата поддержки

# Опциональные
ADMIN_PASSWORD=admin123
ADMIN_CHAT_ID=1059138125
WEBAPP_URL=https://your-app.onrender.com
```

---

**Дата обновления:** 2025-12-09  
**Версия документа:** 1.0  
**Статус:** Актуально
